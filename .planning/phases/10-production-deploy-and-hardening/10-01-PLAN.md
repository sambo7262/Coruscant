---
phase: 10-production-deploy-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/docker-publish.yml
  - packages/backend/src/__tests__/plex-adapter.test.ts
  - packages/backend/src/__tests__/nas-adapter.test.ts
  - packages/backend/src/__tests__/arr-webhooks.test.ts
  - packages/backend/src/__tests__/sse.test.ts
autonomous: true
requirements: [PROD-01]
must_haves:
  truths:
    - "CI workflow triggers on v* tag push and emits both semver and latest Docker tags"
    - "All 19 test files pass with 0 failures (was 16 failures across 4 files)"
  artifacts:
    - path: ".github/workflows/docker-publish.yml"
      provides: "Version tag CI trigger"
      contains: "tags: ['v*']"
    - path: "packages/backend/src/__tests__/plex-adapter.test.ts"
      provides: "Fixed Plex adapter tests"
    - path: "packages/backend/src/__tests__/nas-adapter.test.ts"
      provides: "Fixed NAS adapter tests"
    - path: "packages/backend/src/__tests__/arr-webhooks.test.ts"
      provides: "Fixed arr webhook tests"
    - path: "packages/backend/src/__tests__/sse.test.ts"
      provides: "Fixed SSE tests"
  key_links:
    - from: ".github/workflows/docker-publish.yml"
      to: "Docker Hub"
      via: "on.push.tags trigger"
      pattern: "tags:.*v\\*"
---

<objective>
Fix all 16 pre-existing test failures and update the CI workflow to support version tag builds.

Purpose: Tests must be green before making production changes (D-11). CI must support v* tag triggers so `git push origin v1.0.0` builds a versioned image (D-01, D-05).
Output: Green test suite, CI workflow with version tag support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-deploy-and-hardening/10-CONTEXT.md
@.planning/phases/10-production-deploy-and-hardening/10-RESEARCH.md

<interfaces>
<!-- Key source files the executor needs to understand -->

From packages/backend/src/adapters/plex.ts (fetchPlexServerStats):
- Uses `processCpuUtilization` and `processMemoryUtilization` fields (not `cpuPercentage`/`physMemMB`)
- Takes the LAST entry from StatisticsResources array (not the first)
- Returns `{ processCpuPercent, processRamPercent, bandwidthMbps }` where bandwidthMbps = Math.round((sessionBandwidthKbps / 1000) * 10) / 10

From packages/backend/src/adapters/nas.ts (fetchNasDockerStats):
- Iterates over two API namespaces: SYNO.Docker.Container.Resource then SYNO.ContainerManager.Container.Resource
- Uses axios.get().then() chain — the mock must return a Promise (resolved via mockResolvedValue), not a sync value
- Returns NasDockerStats = { cpuPercent, ramPercent } aggregated from resources[] array

From packages/backend/src/routes/arr-webhooks.ts:
- Currently logs with `{ service: service.toUpperCase() }` — tests assert on handleArrEvent calls, not log format
- The 5 test failures are from arr-webhooks.test.ts but the research says they're "stale logging format assertions"

From packages/backend/src/__tests__/sse.test.ts:
- 6 failures from `ERR_HTTP_HEADERS_SENT` — Fastify inject() + SSE reply.raw streaming conflict
- Need to investigate whether the test creates SSE responses that are never properly closed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 16 pre-existing test failures across 4 test files</name>
  <files>
    packages/backend/src/__tests__/plex-adapter.test.ts
    packages/backend/src/__tests__/nas-adapter.test.ts
    packages/backend/src/__tests__/arr-webhooks.test.ts
    packages/backend/src/__tests__/sse.test.ts
  </files>
  <read_first>
    packages/backend/src/adapters/plex.ts
    packages/backend/src/adapters/nas.ts
    packages/backend/src/routes/arr-webhooks.ts
    packages/backend/src/routes/sse.ts
    packages/backend/src/__tests__/plex-adapter.test.ts
    packages/backend/src/__tests__/nas-adapter.test.ts
    packages/backend/src/__tests__/arr-webhooks.test.ts
    packages/backend/src/__tests__/sse.test.ts
  </read_first>
  <action>
    Fix all 16 test failures. The root causes per RESEARCH.md:

    **plex-adapter.test.ts (3 failures):**
    The `fetchPlexServerStats` test helper `makeStatisticsResponse` uses old field names (`cpuPercentage`, `physMemMB`, `totalPhysMemMB`). The actual adapter now reads `processCpuUtilization` and `processMemoryUtilization` (floats 0-100). Also, the adapter takes the LAST entry in the array (most recent), not the first. Update:
    - Change mock entry fields from `{ cpuPercentage, physMemMB, totalPhysMemMB }` to `{ processCpuUtilization, processMemoryUtilization }` (both are already 0-100 floats)
    - The "first entry" test should assert on the LAST entry values, or swap order of entries
    - `bandwidthMbps` computation is `Math.round((sessionBandwidthKbps / 1000) * 10) / 10` — verify expected values match
    - `processRamPercent` rounding test: 333/1000 = 33.3 but the new adapter reads `processMemoryUtilization` directly as a 0-100 float. Use `processMemoryUtilization: 33.3` and expect `33.3`

    **nas-adapter.test.ts (2 failures):**
    The `fetchNasDockerStats` tests may have mock shape issues after quick task 260405-b24 changed the adapter. The adapter now uses `axios.get().then(r => r.data)` chains. Verify the mock setup resolves correctly through the `.then()` chain. Each `mockResolvedValue` must return `{ data: { success: ..., data: { resources: [...] } } }` at the axios response level (the adapter's `.then(r => r.data)` extracts `r.data`). Check the test expectations for `cpuPercent` and `ramPercent` field names match the actual adapter output.

    **arr-webhooks.test.ts (5 failures):**
    Run the tests first to identify the exact failures. The research says "stale logging format assertions" but the test file primarily asserts on `handleArrEvent` calls and HTTP status codes, not log format. The failures may be caused by the `vi.mock('../poll-manager.js')` not exporting `classifyArrEvent` and `extractArrTitle` that the route now imports. Check if the mock needs to provide these exports. The `classifyArrEvent`/`extractArrTitle` tests use `vi.importActual` which should work. Focus on the 5 failing tests in the `arrWebhookRoutes` describe block.

    **sse.test.ts (6 failures):**
    `ERR_HTTP_HEADERS_SENT` is a known Fastify inject() issue with SSE streaming responses. The SSE handler writes to `reply.raw` and Fastify's inject pipeline can conflict. Fix approach: add proper cleanup/abort handling in tests — ensure each test closes the app and waits for pending connections. If the SSE handler uses `reply.raw.write()` after the inject test completes, add `reply.raw.on('close', ...)` guards, or use `app.close()` with await in `afterEach`. The mock socket detection pattern (`request.raw.socket.constructor.name === 'MockSocket'`) should be verified still works.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && npm run test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits with code 0
    - Output contains "0 failed" or "Tests  \d+ passed" with no "failed" count
    - All 19 test files pass
    - plex-adapter.test.ts: all `fetchPlexServerStats` tests pass with updated mock fields (`processCpuUtilization`, `processMemoryUtilization`)
    - nas-adapter.test.ts: `fetchNasDockerStats` tests pass with correct mock shape
    - arr-webhooks.test.ts: all 5 webhook route tests and all classifyArrEvent/extractArrTitle tests pass
    - sse.test.ts: no `ERR_HTTP_HEADERS_SENT` unhandled rejections
  </acceptance_criteria>
  <done>All 173+ tests pass with 0 failures and 0 unhandled errors</done>
</task>

<task type="auto">
  <name>Task 2: Add version tag trigger to CI workflow (D-01, D-02, D-05)</name>
  <files>.github/workflows/docker-publish.yml</files>
  <read_first>
    .github/workflows/docker-publish.yml
    .planning/phases/10-production-deploy-and-hardening/10-RESEARCH.md
  </read_first>
  <action>
    Update `.github/workflows/docker-publish.yml` per D-01, D-02, D-05:

    1. Add tag trigger to `on.push`:
    ```yaml
    on:
      push:
        branches: [main]
        tags: ['v*']
    ```

    2. Update the `docker/metadata-action@v5` tags block to emit three tag types:
    ```yaml
    tags: |
      type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') }}
      type=sha,format=short
      type=semver,pattern=v{{version}}
    ```

    Key details per RESEARCH.md:
    - `type=raw,value=latest` must fire on BOTH branch push to main AND v* tag push (D-02 requires both `v1.0.0` AND `latest` on tag push)
    - `type=semver,pattern=v{{version}}` preserves the `v` prefix (Pitfall 2 from research) so tag `v1.0.0` produces Docker tag `v1.0.0` not `1.0.0`
    - `type=sha,format=short` remains unchanged — sha tags serve as rollback targets (D-19)
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tags:.*v\*" .github/workflows/docker-publish.yml && grep -c "type=semver" .github/workflows/docker-publish.yml && grep -c "startsWith" .github/workflows/docker-publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/docker-publish.yml` contains `tags: ['v*']` under `on.push`
    - `.github/workflows/docker-publish.yml` contains `type=semver,pattern=v{{version}}`
    - `.github/workflows/docker-publish.yml` contains `enable=${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') }}`
    - `.github/workflows/docker-publish.yml` still contains `type=sha,format=short`
    - `.github/workflows/docker-publish.yml` still contains `type=raw,value=latest`
  </acceptance_criteria>
  <done>CI workflow fires on v* tag push and emits `v1.0.0`, `latest`, and `sha-*` Docker tags</done>
</task>

</tasks>

<verification>
- `npm run test` passes with 0 failures
- `grep "tags:.*v\*" .github/workflows/docker-publish.yml` returns a match
- `grep "type=semver" .github/workflows/docker-publish.yml` returns a match
</verification>

<success_criteria>
1. All 19 test files pass with 0 failures (confirmed by `npm run test` exit code 0)
2. CI workflow contains v* tag trigger and semver + latest tag output rules
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-deploy-and-hardening/10-01-SUMMARY.md`
</output>
