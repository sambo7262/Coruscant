---
phase: 10-production-deploy-and-hardening
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - .gitignore
  - compose.yaml
autonomous: false
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "Worktree branches deleted from local repo"
    - ".gitignore contains .DS_Store, .claude/, .planning/"
    - ".planning/ is untracked from git index (stays on disk)"
    - "Stray files (.DS_Store, '.env.example copy') removed from tracking"
    - "compose.yaml pins image to sambo7262/coruscant:v1.0.0"
    - "v1.0.0 tag exists and is pushed to origin"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules"
      contains: ".planning/"
    - path: "compose.yaml"
      provides: "Pinned version image reference"
      contains: "sambo7262/coruscant:v1.0.0"
  key_links:
    - from: "compose.yaml"
      to: "Docker Hub image"
      via: "image: sambo7262/coruscant:v1.0.0"
      pattern: "sambo7262/coruscant:v1.0.0"
---

<objective>
Clean up the repository, pin compose.yaml to v1.0.0, tag v1.0.0, and verify the deploy works.

Purpose: D-03 (compose pin), D-06-D-10 (git cleanup), D-10 (tag v1.0.0), D-18 (deploy verification). This is the final step before v1.0 ships.
Output: Clean repo, pinned compose, v1.0.0 tag on origin, verified working deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-deploy-and-hardening/10-CONTEXT.md
@.planning/phases/10-production-deploy-and-hardening/10-RESEARCH.md
@.planning/phases/10-production-deploy-and-hardening/10-01-SUMMARY.md
@.planning/phases/10-production-deploy-and-hardening/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git cleanup + .gitignore + compose.yaml pin (D-03, D-06, D-07, D-08, D-09, D-10)</name>
  <files>
    .gitignore
    compose.yaml
  </files>
  <read_first>
    .gitignore
    compose.yaml
    .planning/phases/10-production-deploy-and-hardening/10-CONTEXT.md
    .planning/phases/10-production-deploy-and-hardening/10-RESEARCH.md
  </read_first>
  <action>
    Execute the following git cleanup steps in order. D-10 says keep full git history, no squashing.

    **Step 1: Delete worktree branches (D-06)**
    The 5 worktree branches exist locally only (already gone from remote per `git branch -r` check):
    ```
    worktree-agent-a2e5d0df
    worktree-agent-abc7cc09
    worktree-agent-ac1a1b09
    worktree-agent-ac7b944f
    worktree-agent-adf1cb13
    ```
    Delete local branches: `git branch -D worktree-agent-a2e5d0df worktree-agent-abc7cc09 worktree-agent-ac1a1b09 worktree-agent-ac7b944f worktree-agent-adf1cb13`
    Note: These are local-only branches (the `+` prefix in `git branch` output indicates they were worktree branches). They are NOT on origin.

    Also remove `.claude/worktrees/` directory if it exists on disk: `rm -rf .claude/worktrees/`

    **Step 2: Update .gitignore (D-09)**
    Append the following to the EXISTING `.gitignore` (do not remove existing entries):
    ```
    # Production cleanup (Phase 10)
    .DS_Store
    .claude/
    .planning/
    ```

    **Step 3: Untrack .planning/ from git index (D-07)**
    Run: `git rm -r --cached .planning/`
    This untracks all planning files from the index. They remain on disk and in git history. Verify with `git status` — .planning/ files should show as "deleted" in staged changes but still exist on disk.

    **Step 4: Remove stray tracked files (D-08)**
    Run: `git rm -f .DS_Store` (if tracked)
    Run: `git rm -f ".env.example copy"` (if tracked — note the space in filename, use quotes)
    These files show as modified/untracked in `git status`. Remove them from tracking and disk.

    **Step 5: Update compose.yaml (D-03)**
    Change line 3 from:
    ```yaml
    image: ${DOCKER_HUB_REPO}:${IMAGE_TAG:-latest}
    ```
    To:
    ```yaml
    image: sambo7262/coruscant:v1.0.0
    ```
    Per RESEARCH.md: hardcode the image name rather than env-var-driven so the version is explicit in the file and visible in git history. Rollback procedure (D-19): change this line to a prior `sha-*` tag.

    **Step 6: Commit all cleanup changes**
    Stage: `.gitignore`, `compose.yaml`, and the `git rm` staged deletions.
    Commit message: `chore(v1.0): git cleanup, .gitignore update, compose pin to v1.0.0`

    **Step 7: Tag v1.0.0 (D-01, D-10)**
    Run: `git tag v1.0.0`
    Do NOT push the tag yet — the checkpoint in Task 2 will confirm with the user first.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && git branch | grep -c worktree; echo "---"; grep ".planning/" .gitignore; echo "---"; grep "sambo7262/coruscant:v1.0.0" compose.yaml; echo "---"; git tag -l v1.0.0</automated>
  </verify>
  <acceptance_criteria>
    - `git branch | grep worktree` returns 0 matches (all 5 deleted)
    - `.gitignore` contains `.DS_Store`, `.claude/`, `.planning/` entries
    - `.planning/` files exist on disk but `git ls-files .planning/` returns empty (untracked)
    - `.DS_Store` no longer tracked: `git ls-files .DS_Store` returns empty
    - `".env.example copy"` no longer tracked or on disk
    - `compose.yaml` line 3 is exactly `image: sambo7262/coruscant:v1.0.0`
    - `git tag -l v1.0.0` returns `v1.0.0`
    - `git log --oneline -1` shows the cleanup commit
  </acceptance_criteria>
  <done>Repo cleaned, .gitignore updated, .planning untracked, compose pinned, v1.0.0 tagged locally</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy verification — push tag and confirm (D-01, D-18)</name>
  <files>none — verification only</files>
  <action>
    Present the user with a summary of all Phase 10 changes and ask them to push the v1.0.0 tag and verify the deploy. All code changes are committed. The v1.0.0 tag is created locally but NOT pushed — the user decides when to push.

    Changes completed across all 3 plans:
    - CI workflow updated to build versioned images on v* tag push (Plan 01)
    - 16 pre-existing test failures fixed, full suite green (Plan 01)
    - Webhook logging hardened with 'webhook' filter category (Plan 02)
    - Pi-hole BPM bug fixed (frequency * 60) (Plan 02)
    - MediaStackRow LED glow fixed (Plan 02)
    - Git cleanup: worktree branches deleted, stale files removed, .gitignore updated (this plan)
    - compose.yaml pinned to sambo7262/coruscant:v1.0.0
    - v1.0.0 tag created locally (not yet pushed)
  </action>
  <verify>User confirms deploy works after pushing tag and pulling image on NAS</verify>
  <acceptance_criteria>
    - User types "approved" after verifying dashboard loads on NAS
  </acceptance_criteria>
  <done>v1.0.0 deployed and verified on Synology NAS</done>
  <what-built>
    All Phase 10 work is complete. The v1.0.0 tag is created locally and ready to push.
  </what-built>
  <how-to-verify>
    1. Review the changes: `git log --oneline -5` to see recent commits
    2. Verify tests pass: `npm run test`
    3. When ready, push the tag: `git push origin v1.0.0`
    4. Wait for GitHub Actions to complete (check Actions tab or `gh run list`)
    5. Verify Docker Hub has both `v1.0.0` and `latest` tags at https://hub.docker.com/r/sambo7262/coruscant/tags
    6. On the Synology NAS: `docker compose pull && docker compose up -d`
    7. Open the dashboard in a browser and confirm it loads with all tiles rendering
    8. Check the log viewer — webhook entries should show a 'webhook' filter option (if any webhooks have fired)
  </how-to-verify>
  <resume-signal>Type "approved" to finalize v1.0, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `git branch | grep worktree` returns empty
- `grep ".planning/" .gitignore` returns a match
- `grep "sambo7262/coruscant:v1.0.0" compose.yaml` returns a match
- `git tag -l v1.0.0` returns v1.0.0
- User confirms deploy works in browser (D-18)
</verification>

<success_criteria>
1. All 5 worktree branches deleted locally
2. .gitignore blocks .DS_Store, .claude/, .planning/ from future tracking
3. .planning/ untracked from git index (stays on disk)
4. compose.yaml references `sambo7262/coruscant:v1.0.0` (no env vars)
5. v1.0.0 tag pushed to origin, CI builds versioned image
6. User confirms dashboard loads on NAS after deploy
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-deploy-and-hardening/10-03-SUMMARY.md`
</output>
