---
phase: 05-ui-v2-instrument-panel-polish
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/frontend/src/components/cards/CardGrid.tsx
  - packages/frontend/src/components/layout/NowPlayingBanner.tsx
  - packages/frontend/src/pages/DashboardPage.tsx
  - packages/frontend/src/index.css
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-07, DASH-08]

must_haves:
  truths:
    - "Full dashboard (header + card grid + bottom rail) fits within 800x480 without vertical or horizontal scrolling (verified by human checkpoint Task 2)"
    - "All cards are visible and tappable at 800x480 viewport (verified by human checkpoint Task 2)"
    - "Scrollbar does NOT appear on dashboard page at 800x480 (verified by human checkpoint Task 2)"
    - "Touch targets are at least 44x44px effective area (verified by human checkpoint Task 2)"
    - "Animated grid background renders correctly and is not broken by layout changes (DASH-02, verified by human checkpoint Task 2)"
    - "Card border trace animations are intact and unaffected by Phase 5 layout changes (DASH-03, verified by human checkpoint Task 2)"
  artifacts:
    - path: "packages/frontend/src/components/layout/AppHeader.tsx"
      provides: "Height-optimized NAS header"
    - path: "packages/frontend/src/components/cards/CardGrid.tsx"
      provides: "Compact card grid fitting viewport budget"
  key_links:
    - from: "packages/frontend/src/pages/DashboardPage.tsx"
      to: "AppHeader + CardGrid + NowPlayingBanner"
      via: "Flex column layout consuming 480px total"
      pattern: "overflow.*hidden"
---

<objective>
Enforce the 800x480 no-scroll constraint across the entire dashboard. Audit and adjust component heights so header + card grid + bottom rail = 480px total with no scrollbar. Visually verify that the animated grid background (DASH-02) and card border trace animations (DASH-03) remain intact after all Phase 5 layout changes.

Purpose: D-01 hard constraint — the Raspberry Pi kiosk viewport is 800x480 landscape and the dashboard must fit without scrolling. This is the final gate for the phase.
Output: Viewport-budgeted dashboard layout + visual verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-v2-instrument-panel-polish/05-CONTEXT.md
@.planning/phases/05-ui-v2-instrument-panel-polish/05-02-SUMMARY.md
@.planning/phases/05-ui-v2-instrument-panel-polish/05-03-SUMMARY.md
@.planning/phases/05-ui-v2-instrument-panel-polish/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and enforce 480px vertical budget across all dashboard components</name>
  <files>
    packages/frontend/src/components/layout/AppHeader.tsx
    packages/frontend/src/components/cards/CardGrid.tsx
    packages/frontend/src/components/layout/NowPlayingBanner.tsx
    packages/frontend/src/pages/DashboardPage.tsx
    packages/frontend/src/index.css
  </files>
  <read_first>
    packages/frontend/src/components/layout/AppHeader.tsx
    packages/frontend/src/components/cards/CardGrid.tsx
    packages/frontend/src/components/layout/NowPlayingBanner.tsx
    packages/frontend/src/pages/DashboardPage.tsx
    packages/frontend/src/index.css
  </read_first>
  <action>
**Pixel budget (D-01, D-02):**
```
Total viewport height:       480px
AppHeader (fixed top):       ~128px max (44px title + ~84px NAS sections)
NowPlayingBanner (fixed bottom): 40px
Available for CardGrid:      480 - 128 - 40 = ~312px
```

**Step 1: Ensure DashboardPage main content area uses the remaining space**

The main content area (where CardGrid renders) must account for the fixed header and fixed bottom rail. Add explicit padding-top and padding-bottom to the main content wrapper in DashboardPage.tsx:

```tsx
<main style={{
  paddingTop: '128px',   // Match header height — adjust based on actual measured header
  paddingBottom: '40px', // Match rail height
  height: '480px',
  overflow: 'hidden',    // NO scrolling (D-01)
  boxSizing: 'border-box',
}}>
  <CardGrid snapshot={snapshot} />
</main>
```

If DashboardPage doesn't have this wrapper, add it around the CardGrid component.

Note: The actual header height depends on how many NAS sections render (disks, docker, image LED). The `paddingTop` value should match the real header height. Start with 128px and adjust after visual inspection.

**Step 2: Audit and compact AppHeader if over budget**

If AppHeader exceeds ~128px with all sections visible, apply these reductions:
- Reduce disk temp bars: GaugeColumn height from 20px to 14px bars
- Reduce gap between sections from 4px to 2px
- Reduce font sizes in DISKS/DOCKER labels from 8px to 7px
- Combine image update LED with Docker row if possible (same line)

**Step 3: Audit and compact CardGrid if cards don't fit in ~312px**

With tier section labels already removed (Plan 02), cards should fit in ~312px. But verify:
- Card `minmax(140px, 1fr)` grid sizing may need reduction to `minmax(120px, 1fr)` if too few columns render
- Card internal padding may need reduction from `8px` gaps to `6px`
- ServiceCard component height: check the chamfered card's total height. If over ~140px, reduce internal padding from the instrument body

**Step 4: Audit NowPlayingBanner**

Confirm collapsed rail is 40px (done in Plan 04). No additional changes needed.

**Step 5: Add CSS overflow protection**

In `index.css`, add:

```css
/* Prevent body scroll on kiosk viewport (D-01) */
body {
  overflow: hidden;
  height: 100vh;
  max-height: 480px; /* kiosk target — overridden by larger viewports naturally */
}

@media (min-height: 481px) {
  body {
    max-height: none;
    overflow: auto;
  }
}
```

This ensures at exactly 480px viewport height, the body cannot scroll. On larger viewports (desktop, phones in portrait), normal scrolling is allowed.

**Step 6: Verify animated grid background is intact (DASH-02)**

After layout changes, confirm the animated grid background CSS/component still renders. Check that no `z-index` or `overflow: hidden` changes clip the grid. If the grid background component exists (check for GridBackground, AnimatedGrid, or similar in the components directory), ensure it is still rendered in DashboardPage and not hidden behind other elements.

**Step 7: Verify card border trace animations are intact (DASH-03)**

Confirm that the chamfered card border trace CSS animations (the glowing border effect on ServiceCard) are not broken by any layout or overflow changes. The `chamfer-card` clip-path and any `@keyframes borderTrace` should still be present in index.css and applied to cards.

**Step 8: Test with `npm run build` to confirm no build errors**

Run `npm run build` to confirm the full application builds cleanly after all Phase 5 changes.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && npm run build 2>&1 | tail -10 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard layout enforces 480px vertical budget. Body scroll is disabled at kiosk viewport. Animated grid background and card border trace animations are confirmed present. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 5 dashboard at 800x480</name>
  <files>packages/frontend/src/pages/DashboardPage.tsx</files>
  <action>
This is a human verification checkpoint. Claude starts the dev server and provides the URL. The user visually inspects the dashboard at 800x480 viewport in Chrome DevTools. No code changes are made in this task unless issues are found.

Run: `cd /Users/Oreo/Projects/Coruscant && npm run dev`
Provide the URL to the user for inspection.
  </action>
  <verify>User confirms visual approval by typing "approved"</verify>
  <what-built>
    Complete Phase 5 UI rework:
    - Arr services in 2-column chamfered tile with green/amber/red/purple LEDs
    - SABnzbd card with amber text, filename/speed/ETA body (no progress bar)
    - NETWORK card (renamed from Pi-hole) with Ubiquiti placeholder
    - NAS header always-visible with disk temp bars (F), Docker stats, image LED
    - Plex rail with "PLEX" label, cycling titles, server stats
    - StreamRow with AUDIO/VIDEO badge and correct audio display
    - No-scroll enforcement at 800x480
    - Animated grid background intact (DASH-02)
    - Card border trace animations intact (DASH-03)
    - SABnzbd detail view with active job + queue list (D-08)
    - Arr detail view with dot-leader rows + health warnings (D-14)
    - Pi-hole detail view with static table legend (D-17)
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/Oreo/Projects/Coruscant && npm run dev`
    2. Open Chrome DevTools, Device Toolbar, set custom device 800x480 landscape
    3. Navigate to dashboard (http://localhost:5173 or similar)
    4. Verify NO scrollbar appears — all content fits within the viewport
    5. Verify animated grid background is visible behind the cards (DASH-02)
    6. Verify card border trace animations are present on chamfered cards (DASH-03)
    7. Check arr tile: chamfered card with 2 columns, LEDs should be green (if online + idle)
    8. Check SABnzbd card: text should be amber (not purple), shows filename + speed + ETA (no progress bar)
    9. Check NETWORK card: header reads "NETWORK", has PI-HOLE section + UBIQUITI "NOT CONFIGURED"
    10. Check NAS header: stats strip + disk temp bars (F) + Docker stats + image LED all visible without tapping
    11. Check Plex rail: "PLEX" on left, title cycling in center, server stats on right (if streams active)
    12. If Plex streams active: expand rail, verify AUDIO/VIDEO badges on stream rows
    13. Tap SABnzbd card to verify detail view shows active job + queue list (D-08)
    14. Tap an arr service card to verify detail view shows status/version dot-leaders + health warnings (D-14)
    15. Tap NETWORK card to verify Pi-hole detail view has table legend (not tooltip) (D-17)
    16. Tap a service card to verify navigation to detail view still works
  </how-to-verify>
  <done>User has visually verified all Phase 5 changes at 800x480 viewport including animated grid background (DASH-02), card border trace animations (DASH-03), and all card/detail view reworks. User approved.</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run test` passes
- Visual: 800x480 viewport shows full dashboard without scrolling
- Visual: animated grid background visible (DASH-02)
- Visual: card border trace animations working (DASH-03)
</verification>

<success_criteria>
- Dashboard fits 800x480 without scrolling
- Animated grid background renders correctly (DASH-02)
- Card border trace animations are intact (DASH-03)
- All Phase 5 visual changes are cohesive at kiosk viewing distance
- Touch targets are comfortable
- No obvious visual bugs
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-v2-instrument-panel-polish/05-05-SUMMARY.md`
</output>
