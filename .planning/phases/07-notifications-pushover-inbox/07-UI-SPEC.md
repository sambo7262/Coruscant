---
phase: 7
slug: notifications-pushover-inbox
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for the arr webhook event-driven signaling system. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Note: Despite the phase name "Pushover Inbox", this phase delivers a webhook receiver + ephemeral UI signaling system — not an inbox. See 07-CONTEXT.md D-01 through D-03.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — raw CSS custom properties in `globals.css` |
| Icon library | lucide-react (already installed; `Settings`, `List` in AppHeader) |
| Font | JetBrains Mono — `var(--font-mono)` |

No shadcn. The project uses hand-authored CSS custom properties established in Phase 2. All new components in Phase 7 extend that existing token set.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, LED-to-label gap, inline padding in ticker |
| sm | 8px | Gap between ticker segments, compact element spacing |
| md | 16px | Default element spacing, section padding |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Ticker overlay uses 12px horizontal padding (matches existing AppHeader `padding: 0 12px`)
- Card label flash border is 1px — not on the scale; border widths are exempt
- Copy button in Settings Notifications tab uses 8px horizontal padding, 4px vertical (compact inline button)

---

## Typography

All type uses `var(--font-mono)` (JetBrains Mono). Established in `globals.css`.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 400 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 20px | 600 | 1.2 |

Phase 7 additions:

| Role | Size | Weight | Letter Spacing | Usage |
|------|------|--------|----------------|-------|
| Ticker text | 12px | 400 | 0.08em | AppHeader ticker overlay — `SERVICE ▸ EVENT ▸ TITLE` |
| Webhook URL | 11px | 400 | 0.04em | Settings Notifications tab URL display |
| Webhook label | 10px | 400 | 0.06em | Service name label above each URL row |

Source: `globals.css` established values. Ticker and webhook sizes derived from existing 12px label role.

---

## Color

Established palette from `globals.css`. Phase 7 adds event-semantic flash colors.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#080C14` (`--space-deep`) | Body background, page surface |
| Secondary (30%) | `#0D0D0D` (`--bg-panel`) + `#0E1520` (`--space-mid`) | Cards, header, settings tab backgrounds |
| Accent (10%) | `#E8A020` (`--cockpit-amber`) | Service labels, gauge bars, border traces, LED normal state, ticker text, copy button hover |
| Destructive | `#FF3B3B` (`--cockpit-red`) | Offline LED, health_issue card flash, error states |

Accent reserved for: service name labels (e.g. "RADARR"), gauge fill bars, card border glow lines, status LED normal state, AppHeader title "CORUSCANT", ticker overlay text, copy button hover ring.

### Phase 7 Event Flash Colors (NEW)

These colors are additive to the existing palette — used only during the 10-second flash window on card label boxes:

| Event Type | Flash Color | Hex | Semantic |
|------------|-------------|-----|----------|
| `grab` | Amber | `#ffaa00` | Item grabbed for download (matches `--cockpit-amber` warning register) |
| `download_complete` / `import` | Purple | `#c084fc` | File imported to library (matches existing `--cockpit-purple` download state) |
| `health_issue` | Red | `#ff4444` | Arr health check failed (matches `--cockpit-red`) |
| `update_available` | Green | `#00ff88` | App update available (matches `--cockpit-green`) |

Source: 07-CONTEXT.md D-07. Flash colors align with existing LED semantic colors already in `globals.css`.

### Ticker Overlay Colors

| Element | Color |
|---------|-------|
| Ticker background | `rgba(13, 13, 13, 0.97)` — same as AppHeader background at higher opacity |
| Ticker text | `var(--cockpit-amber)` |
| Ticker chevron separator `▸` | `rgba(232, 160, 32, 0.5)` — dimmed to reduce visual weight |
| Ticker border-bottom (replaces header border during overlay) | Event flash color for active event |

---

## Component Inventory

### New: Card Label Flash Animation

**Location:** `packages/frontend/src/components/cards/ServiceCard.tsx` — applied to the arr card label box (the amber mono service name header, e.g. "RADARR").

**Behavior:** When an `arr_event` SSE message arrives for a service, the card label box receives a colored border/glow flash for 10 seconds, then fades out.

**Visual spec:**
- Border: `1px solid {event-color}` on all four sides of the label box
- Box shadow: `0 0 8px 2px {event-color}@40%` (glow halo)
- Animation: `flashGlow` keyframe — 0s: full opacity glow → 8s: still visible → 10s: opacity 0
- Timing: `ease-out` curve on the fade, linear hold for first 8 seconds
- CSS keyframe name: `@keyframes arrFlash` — add to `globals.css`

```css
/* Add to globals.css — arr card label flash */
@keyframes arrFlash {
  0%, 80%  { opacity: 1; }
  100%     { opacity: 0; }
}
```

The flash class is toggled via React state, not a CSS-only approach, so the animation can be re-triggered on rapid successive events (see debounce note below).

**Debounce:** If two events for the same service arrive within 500ms, show only the second flash (reset the timer). This prevents double-flash visual noise on rapid grab sequences.

### New: AppHeader Ticker Overlay

**Location:** `packages/frontend/src/components/layout/AppHeader.tsx` — overlays the existing center/right columns of the title row for 10 seconds.

**Visual spec:**
- Overlay covers only the center and right thirds of the 3-column header grid (`gridTemplateColumns: '1fr auto 1fr'`)
- Left column (CORUSCANT title / back link) is always visible — never covered
- Ticker text: `{SERVICE} ▸ {EVENT} ▸ {TITLE}` — 12px mono, amber
- Title portion omitted for events with no title: `PROWLARR ▸ INDEXER DOWN`
- Overflow: `hidden` with `text-overflow: ellipsis` — max width is the two right columns
- Text is static (no marquee scroll) — if too long, ellipsis truncates
- After 10s: ticker fades out (`opacity: 0`, `transition: opacity 0.4s ease-out`), header snaps back to normal state
- Bottom border of ticker row changes to event flash color during the overlay period, then reverts to `rgba(232, 160, 32, 0.30)`

**States:**
- `idle` — normal AppHeader renders, no ticker
- `active` — ticker overlay visible, event color accent on bottom border
- `fading` — 0.4s opacity fade before returning to `idle`

### New: Settings Notifications Tab

**Location:** `packages/frontend/src/pages/SettingsPage.tsx` — new "NOTIFICATIONS" tab added to existing tabbed settings layout.

**Visual spec:**
- Tab label: `NOTIFICATIONS` (uppercase, mono, matches existing tab style)
- Tab content: read-only list — no forms, no credentials, no TEST CONNECTION button (D-19)
- Each row: service name label + URL display field + COPY button
- Row layout:

```
RADARR
http://192.168.1.x:1688/api/webhooks/radarr  [COPY]
```

- Service label: 10px mono, `rgba(232,160,32,0.6)`, uppercase
- URL field: 11px mono, `rgba(200,200,200,0.8)`, `background: rgba(255,255,255,0.04)`, `border: 1px solid rgba(232,160,32,0.15)`, padding `4px 8px`, border-radius `2px`
- COPY button: 10px mono, `color: var(--cockpit-amber)`, `border: 1px solid rgba(232,160,32,0.3)`, padding `4px 8px`, hover: `border-color: rgba(232,160,32,0.7)`, active: brief `background: rgba(232,160,32,0.1)`
- Copy success state: button text changes to `COPIED` for 1.5 seconds, then reverts to `COPY`
- URL source: uses configured Coruscant base URL from settings; falls back to placeholder `http://{coruscant-ip}:1688/api/webhooks/{service}` if base URL not set

**Services listed (in order):** RADARR, SONARR, LIDARR, BAZARR, PROWLARR, READARR, SABNZBD

**No empty state** — all seven services always show their webhook URLs regardless of whether the service is configured. Webhook URLs are fixed paths that don't require service configuration.

---

## Interaction States

### Card Label Flash — All States

| State | Visual |
|-------|--------|
| `idle` | Normal amber label box — no border change |
| `flash-grab` | Amber `#ffaa00` border + glow, 10s then fade |
| `flash-import` | Purple `#c084fc` border + glow, 10s then fade |
| `flash-health` | Red `#ff4444` border + glow, 10s then fade |
| `flash-update` | Green `#00ff88` border + glow, 10s then fade |

Flash only applies to arr service label boxes (RADARR, SONARR, LIDARR, BAZARR, PROWLARR, READARR). SABnzbd does not receive flash (SABnzbd responds to grab via burst poll mode, not a card flash).

### AppHeader Ticker — All States

| State | Visual |
|-------|--------|
| `idle` | Normal AppHeader — connection status center, nav icons right |
| `active` | Ticker text replaces center+right columns, event-color bottom border |
| `fading` | 0.4s ease-out fade on ticker, nav icons fade in simultaneously |

Ticker queue: if a second event fires while ticker is active, immediately replace ticker content (reset 10s timer). No queue — latest event wins.

### Settings COPY Button — All States

| State | Visual |
|-------|--------|
| `default` | `COPY` label, amber border at 30% opacity |
| `hover` | amber border at 70% opacity |
| `active (click)` | `rgba(232,160,32,0.1)` background flash |
| `copied` | Text changes to `COPIED`, 1.5s duration, then reverts |

---

## Animation Spec

### `arrFlash` keyframe

```css
@keyframes arrFlash {
  0%, 80%  { opacity: 1; }
  100%     { opacity: 0; }
}
```

- Duration: `10s`
- Timing: `linear` (flat hold, then drop at 80%)
- Fill mode: `forwards` (stays at opacity 0 after completion)
- Applied to: the glow overlay element layered on top of the card label box

### Ticker Fade-Out

- Property: `opacity 0 → 1` on enter (no transition, instant appear), `opacity 1 → 0` on exit
- Duration: `0.4s`
- Timing: `ease-out`
- Nav icon restore: same `0.4s ease-out` running simultaneously with ticker fade

### COPY Success Flash

- Button text swap: instant (no transition)
- Revert delay: `1500ms` via `setTimeout`

All animations use `transform` and `opacity` only (DASH-08 compliance). No layout-triggering properties animated.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | `COPY` (Settings Notifications tab copy button) |
| Copy success state | `COPIED` (1.5s then reverts to `COPY`) |
| Settings tab label | `NOTIFICATIONS` |
| Webhook URL placeholder (no base URL configured) | `http://{coruscant-ip}:1688/api/webhooks/{service}` |
| Settings tab header copy | `Configure arr apps to POST events to these endpoints. Paste the URL into each app's Connections settings.` (14px body, `--text-offwhite`) |
| Ticker format — grab | `{SERVICE} ▸ GRABBED ▸ {Title}` |
| Ticker format — import | `{SERVICE} ▸ IMPORTED ▸ {Title}` |
| Ticker format — health issue | `{SERVICE} ▸ HEALTH ISSUE` or `PROWLARR ▸ INDEXER DOWN` |
| Ticker format — update available | `{SERVICE} ▸ UPDATE AVAILABLE` |
| Empty state | None — no empty state exists; the webhook system is passive and always active |
| Error state | None — webhook endpoints are fire-and-forget; frontend never shows an error from a webhook failure |
| Destructive actions | None — Phase 7 has no destructive actions |

Service name in ticker is always uppercase: `RADARR`, `SONARR`, `LIDARR`, `BAZARR`, `PROWLARR`, `READARR`. Event verbs are uppercase: `GRABBED`, `IMPORTED`, `HEALTH ISSUE`, `UPDATE AVAILABLE`, `INDEXER DOWN`. Title is title-case as returned by the arr webhook payload.

---

## Accessibility

- Flash animations respect `prefers-reduced-motion: reduce` via the existing global rule in `globals.css` that sets `animation-duration: 0.001ms` for all animations. Flash effect will be near-instantaneous but the color change state will still render.
- Ticker overlay: aria-live region recommended — `aria-live="polite"` on the ticker container so screen readers announce new events.
- COPY button: `aria-label="Copy webhook URL for {service}"` on each button instance.
- All touch targets: COPY button minimum 32px height (adequate for settings page use; this is a settings-page-only control, not a kiosk primary interaction).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No third-party component registry used. All components are bespoke extensions of the existing `ServiceCard.tsx` and `AppHeader.tsx` patterns.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| No shadcn / no Tailwind — raw CSS custom properties | Codebase scan: `globals.css`, `AppHeader.tsx` |
| Color palette (`--cockpit-amber`, `--cockpit-red`, etc.) | Codebase scan: `globals.css` `:root` block |
| Typography roles (12/14/16/20px, 400/600 weight) | Codebase scan: `globals.css` `.text-*` classes |
| Spacing (12px header padding, 4px/8px gaps) | Codebase scan: `AppHeader.tsx` inline styles |
| Event flash colors (amber/purple/red/green) | 07-CONTEXT.md D-07 |
| Flash duration 10 seconds | 07-CONTEXT.md D-09 |
| Ticker overlay format `SERVICE ▸ EVENT ▸ TITLE` | 07-CONTEXT.md D-13 |
| Ticker covers center+right, not left column | 07-CONTEXT.md D-12 |
| No TEST CONNECTION on Notifications tab | 07-CONTEXT.md D-19 |
| Webhook URL format | 07-CONTEXT.md D-18 |
| Services listed: Radarr/Sonarr/Lidarr/Bazarr/Prowlarr/Readarr/SABnzbd | 07-CONTEXT.md D-04 |
| No inbox, no persistent notification UI | 07-CONTEXT.md D-02 |
| Framer Motion available (motion wrapper on cards) | Codebase scan: `ServiceCard.tsx` import |
| Icon library: lucide-react | Codebase scan: `AppHeader.tsx` imports |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
