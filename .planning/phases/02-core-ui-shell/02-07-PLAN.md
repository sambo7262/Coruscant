---
phase: 02-core-ui-shell
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/ui/StatusDot.tsx
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/frontend/src/pages/SettingsPage.tsx
  - packages/frontend/src/pages/LogsPage.tsx
autonomous: true
gap_closure: true
requirements:
  - DASH-04
  - DASH-07
  - DASH-01

must_haves:
  truths:
    - "StatusDot renders as 10px round LED with radial glow and breathing/pulse animation per health state"
    - "AppHeader uses amber cockpit chrome — no cyan/Tron blue anywhere"
    - "AppHeader accepts showBack prop; when true, title becomes tappable link to /"
    - "Settings and Logs pages render AppHeader with showBack=true"
  artifacts:
    - path: "packages/frontend/src/components/ui/StatusDot.tsx"
      provides: "LED status indicator with radial glow"
      contains: "cockpit-green"
    - path: "packages/frontend/src/components/layout/AppHeader.tsx"
      provides: "Cockpit-styled header with back navigation"
      contains: "showBack"
    - path: "packages/frontend/src/pages/SettingsPage.tsx"
      provides: "Settings page with AppHeader back nav"
      contains: "showBack"
    - path: "packages/frontend/src/pages/LogsPage.tsx"
      provides: "Logs page with AppHeader back nav"
      contains: "showBack"
  key_links:
    - from: "packages/frontend/src/pages/SettingsPage.tsx"
      to: "packages/frontend/src/components/layout/AppHeader.tsx"
      via: "import and render with showBack"
      pattern: "showBack.*true"
    - from: "packages/frontend/src/pages/LogsPage.tsx"
      to: "packages/frontend/src/components/layout/AppHeader.tsx"
      via: "import and render with showBack"
      pattern: "showBack.*true"
---

<objective>
Restyle StatusDot as cockpit LED indicator and AppHeader as cockpit chrome bar, add back navigation support to AppHeader and wire it into Settings/Logs pages.

Purpose: StatusDot and AppHeader are used across all pages. Restyling them first means every page benefits. Back navigation fixes a usability gap on non-dashboard routes.
Output: Cockpit-styled LED indicators, amber chrome header, back navigation on sub-pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md

@packages/frontend/src/components/ui/StatusDot.tsx
@packages/frontend/src/components/layout/AppHeader.tsx
@packages/frontend/src/pages/SettingsPage.tsx
@packages/frontend/src/pages/LogsPage.tsx
@packages/shared/src/types.ts

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface ServiceStatus {
  id: string
  name: string
  tier: 'status' | 'activity' | 'rich'
  status: 'online' | 'offline' | 'warning' | 'stale'
  lastPollAt: string
  metrics?: Record<string, unknown>
}

export interface NasStatus {
  cpu: number
  ram: number
  volumes: { name: string; usedPercent: number; tempC?: number }[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle StatusDot as cockpit LED with radial glow</name>
  <files>packages/frontend/src/components/ui/StatusDot.tsx</files>
  <read_first>
    - packages/frontend/src/components/ui/StatusDot.tsx
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
Rewrite StatusDot to implement the cockpit LED indicator (per D-12).

**Size:** 10px width and height (was 8px), `border-radius: 50%`.

**Color mapping (per D-12):**
- `online`: `var(--cockpit-green)` (#4ADE80)
- `warning`: `var(--cockpit-amber)` (#E8A020)
- `offline`: `var(--cockpit-red)` (#FF3B3B)
- `stale`: `var(--cockpit-grey)` (#666666)

**Radial glow (per D-12):**
- `online`: `box-shadow: 0 0 8px 3px rgba(74, 222, 128, 0.6)` — green glow
- `warning`: `box-shadow: 0 0 8px 3px rgba(232, 160, 32, 0.6)` — amber glow
- `offline`: `box-shadow: 0 0 8px 3px rgba(255, 59, 59, 0.6)` — red glow
- `stale`: `box-shadow: none` — no glow, dim static

**Animation (per D-12, uses keyframes from globals.css Plan 06):**
- `online`: `animation: ledBreathe 3s ease-in-out infinite`
- `warning`: `animation: ledPulseWarn 1s ease-in-out infinite`
- `offline`: `animation: ledFlashDown 0.4s ease-in-out infinite`
- `stale`: no animation (static)

Keep `aria-hidden="true"`, `display: inline-block`, `flex-shrink: 0`.

Remove all references to `--tron-blue`, `--tron-amber`, `--tron-red`. Use the new `--cockpit-*` vars or direct color values.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tron" packages/frontend/src/components/ui/StatusDot.tsx; grep -q "cockpit-green" packages/frontend/src/components/ui/StatusDot.tsx && echo "PASS: cockpit-green" || echo "FAIL"; grep -q "ledBreathe" packages/frontend/src/components/ui/StatusDot.tsx && echo "PASS: ledBreathe" || echo "FAIL"; grep -q "10px" packages/frontend/src/components/ui/StatusDot.tsx && echo "PASS: 10px size" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "tron" returns 0 matches
    - grep for "cockpit-green" returns a match (online color)
    - grep for "cockpit-amber" or "#E8A020" returns a match (warning color)
    - grep for "cockpit-red" or "#FF3B3B" returns a match (offline color)
    - grep for "ledBreathe" returns a match (online animation)
    - grep for "ledPulseWarn" returns a match (warning animation)
    - grep for "ledFlashDown" returns a match (offline animation)
    - width and height are "10px"
    - aria-hidden="true" is present
  </acceptance_criteria>
  <done>StatusDot renders as 10px round LED with cockpit palette colors, radial glow box-shadow, and health-state-appropriate animations.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle AppHeader as cockpit chrome and add showBack prop</name>
  <files>packages/frontend/src/components/layout/AppHeader.tsx, packages/frontend/src/pages/SettingsPage.tsx, packages/frontend/src/pages/LogsPage.tsx</files>
  <read_first>
    - packages/frontend/src/components/layout/AppHeader.tsx
    - packages/frontend/src/pages/SettingsPage.tsx
    - packages/frontend/src/pages/LogsPage.tsx
    - packages/frontend/src/App.tsx
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
Restyle AppHeader and add back navigation support (per D-21 through D-24, D-37).

**AppHeader interface change:**
Add `showBack?: boolean` to `AppHeaderProps`. Default is `false`.

**When showBack is true (per D-24, D-37):**
- The title text changes to `← CORUSCANT` (left arrow + space + CORUSCANT)
- The title becomes a `<Link to="/">` (from react-router-dom) or a clickable element that navigates to `/`
- Styling: same `text-display` class, amber color, cursor pointer
- Settings and Logs icon buttons are HIDDEN (only shown on dashboard)

**When showBack is false (default, dashboard view):**
- Title is just `CORUSCANT` (no arrow, not a link)
- Settings and Logs buttons visible

**Header restyle (per D-21 through D-23):**
- Title color: `var(--cockpit-amber)` (was `var(--tron-blue)`)
- Icon button color: `var(--cockpit-amber)` (was `var(--tron-blue)`)
- Header background: `rgba(13, 13, 13, 0.95)` with `backdrop-filter: blur(4px)` (per D-23)
- Header border-bottom: `1px solid rgba(232, 160, 32, 0.30)` (per D-22)
- Header box-shadow: `0 1px 8px rgba(232, 160, 32, 0.15)` (per D-22)
- NAS stats strip border-top: `1px solid rgba(232, 160, 32, 0.10)` (was cyan)
- NAS stat values color: `var(--cockpit-amber)` (was `var(--tron-blue)`)
- NAS stat labels color: `var(--text-offwhite)` (was `var(--text-muted)`)
- Connection-lost dot: `var(--cockpit-amber)` with `ledPulseWarn` animation (was `pulseAmber`)

Remove ALL references to `var(--tron-blue)`, `var(--tron-amber)`. Use `var(--cockpit-amber)`, `var(--cockpit-red)`, `var(--text-offwhite)`.

**SettingsPage.tsx update:**
- Import `AppHeader` from `../components/layout/AppHeader.js`
- Import `NasStatus` type if needed
- Render `<AppHeader nas={null} connected={true} showBack={true} />` at the top of the component
- The page no longer manages its own heading — AppHeader provides the back-nav header
- Keep the animation intensity slider but update: label color to `var(--text-offwhite)`, heading color to `var(--cockpit-amber)`
- The `--grid-pulse-opacity` slider label should update to say "LED Intensity" (since grid pulses are gone; this now conceptually controls LED brightness — but the var name stays for Phase 3 migration)

**LogsPage.tsx update:**
- Import `AppHeader` from `../components/layout/AppHeader.js`
- Render `<AppHeader nas={null} connected={true} showBack={true} />` at the top
- Update text colors: heading `var(--cockpit-amber)`, body `var(--text-offwhite)`

**IMPORTANT:** The `App.tsx` already renders `<AppHeader>` at the root level for all routes. The Settings/Logs pages render it ADDITIONALLY with `showBack=true` for the back navigation. This means on Settings/Logs routes there will be two headers. To fix this cleanly: modify App.tsx to pass `showBack` based on current route. Use `useLocation` from react-router-dom — if `location.pathname !== '/'`, set `showBack={true}`.

Actually, simpler approach: Do NOT add AppHeader inside SettingsPage/LogsPage. Instead, in App.tsx, detect the route and pass showBack accordingly. This avoids double-header:
- Import `useLocation` from 'react-router-dom'
- `const showBack = location.pathname !== '/'`
- Pass `showBack={showBack}` to the existing `<AppHeader>` in App.tsx
- When showBack is true, hide Settings/Logs buttons (they're redundant on those pages)

Update App.tsx with this pattern instead of adding AppHeader to individual pages.

For SettingsPage and LogsPage: just update colors/text — no AppHeader import needed since App.tsx handles it.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tron" packages/frontend/src/components/layout/AppHeader.tsx; grep -q "showBack" packages/frontend/src/components/layout/AppHeader.tsx && echo "PASS: showBack prop" || echo "FAIL"; grep -q "cockpit-amber" packages/frontend/src/components/layout/AppHeader.tsx && echo "PASS: cockpit-amber" || echo "FAIL"; grep -q "useLocation" packages/frontend/src/App.tsx && echo "PASS: route detection" || echo "FAIL"; grep -q "showBack" packages/frontend/src/App.tsx && echo "PASS: showBack in App" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "tron" in AppHeader.tsx returns 0 matches
    - grep for "showBack" in AppHeader.tsx returns a match (prop defined)
    - grep for "cockpit-amber" in AppHeader.tsx returns a match
    - grep for "rgba(232, 160, 32, 0.30)" in AppHeader.tsx returns a match (border-bottom)
    - grep for "0 1px 8px rgba(232, 160, 32, 0.15)" in AppHeader.tsx returns a match (box-shadow)
    - App.tsx contains "useLocation" import
    - App.tsx passes "showBack" to AppHeader
    - grep for "tron" in SettingsPage.tsx returns 0 matches
    - grep for "tron" in LogsPage.tsx returns 0 matches
    - grep for "cockpit-amber" or "text-offwhite" in SettingsPage.tsx returns a match
    - When showBack=true, title renders with left arrow character and links to "/"
  </acceptance_criteria>
  <done>AppHeader styled as cockpit chrome with amber palette, showBack prop enables back-nav on sub-pages, Settings/Logs pages use cockpit colors, no Tron references remain.</done>
</task>

</tasks>

<verification>
- `grep -rc "tron" packages/frontend/src/components/ui/StatusDot.tsx packages/frontend/src/components/layout/AppHeader.tsx packages/frontend/src/pages/SettingsPage.tsx packages/frontend/src/pages/LogsPage.tsx` returns all zeros
- `grep "showBack" packages/frontend/src/components/layout/AppHeader.tsx` returns match
- `grep "showBack" packages/frontend/src/App.tsx` returns match
- `cd packages/frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
- StatusDot is a 10px LED with cockpit colors and health-state animations
- AppHeader is amber cockpit chrome with showBack navigation
- App.tsx detects non-dashboard routes and passes showBack=true
- Settings and Logs pages use cockpit palette colors
- Zero Tron references in any modified file
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-07-SUMMARY.md`
</output>
