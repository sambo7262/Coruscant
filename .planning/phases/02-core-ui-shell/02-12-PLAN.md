---
phase: 02-core-ui-shell
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/cards/ServiceCard.tsx
  - packages/frontend/src/pages/ServiceDetailPage.tsx
  - packages/backend/src/mock/generator.ts
autonomous: true
gap_closure: true
requirements:
  - DASH-03
  - DASH-05
  - DASH-07

must_haves:
  truths:
    - "Arr card face shows: service name, LED status, up/down indicator, active-download indicator — nothing else"
    - "Queued/monitored counts removed from card face"
    - "Arr detail page shows: monitored count, queue count, library file sizes, user-attention items"
    - "User-attention items flagged when mock data contains items needing manual import"
  artifacts:
    - path: "packages/frontend/src/components/cards/ServiceCard.tsx"
      provides: "Simplified arr card face"
      contains: "renderArrBody"
    - path: "packages/frontend/src/pages/ServiceDetailPage.tsx"
      provides: "Arr detail page with attention items section"
      contains: "attention"
  key_links: []
---

<objective>
Simplify the Arr service cards (Radarr, Sonarr, Lidarr, Bazarr) on the dashboard: the card face should be minimal — just health status and whether something is actively downloading. Move the rich detail (queue counts, library sizes, manual-import attention items) to the service detail page that opens on tap.

The dot-matrix LED grid was clever but showed too much noise. Replace with a cleaner 2-row instrument readout on the card face:
- Row 1: up/down status (large LED + service name)
- Row 2: active download indicator (a small pulsing amber bar if something is downloading, blank/static if idle)

On the detail page, show the operational data the user actually needs to act on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify Arr card face in ServiceCard</name>
  <files>
    packages/frontend/src/components/cards/ServiceCard.tsx
    packages/backend/src/mock/generator.ts
    packages/shared/src/types.ts
  </files>
  <read_first>
    - packages/frontend/src/components/cards/ServiceCard.tsx
    - packages/backend/src/mock/generator.ts
    - packages/shared/src/types.ts
  </read_first>
  <action>
Replace the dot-matrix LED grid in the arr instrument body with a minimal 2-row readout.

**New arr card face layout:**
```
┌─────────────────────────────┐
│ ▌ RADARR               ● ──┤  ← amber header strip (existing)
├─────────────────────────────┤
│  ● ONLINE                   │  ← large LED + status text
│  ▌▌▌ DOWNLOADING  720p x1  │  ← download bar OR idle state
└─────────────────────────────┘
```

**Design details:**
- Status row: StatusDot (12px) + status text in off-white. If warning/offline, show brief reason (e.g. "DEGRADED", "OFFLINE").
- Download indicator row:
  - **Actively downloading:** small amber animated progress bar (width: percentage of largest active item's progress, or just pulsing if unknown), text label `DOWNLOADING  {quality} x{count}` — e.g. `1080p x2` if two items
  - **Queue idle / nothing downloading:** dim text `IDLE` in grey `#444`, no bar
  - **If Bazarr (subtitle service):** show `SUBTITLES` label and active subtitle grab count instead of download bar
- Remove: dot-matrix grid, monitored count, queue slot display

**Mock generator updates for arr services:**
Add to each arr service mock: `downloading: boolean`, `activeDownloads: number`, `downloadQuality: string` (e.g. "1080p"), `downloadProgress: number` (0–100).
Set Radarr as actively downloading (downloading: true, activeDownloads: 1, downloadQuality: "1080p", downloadProgress: 45) so the state is visible in testing.
  </action>
  <verify>
    - Arr cards show status LED + download state only — no dot matrix, no counts
    - Radarr mock shows download bar pulsing
    - Other arr cards show IDLE state
  </verify>
  <done>Arr card face simplified to status LED + download indicator row.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich Arr detail page with operational data</name>
  <files>
    packages/frontend/src/pages/ServiceDetailPage.tsx
    packages/backend/src/mock/generator.ts
    packages/shared/src/types.ts
  </files>
  <read_first>
    - packages/frontend/src/pages/ServiceDetailPage.tsx
    - packages/backend/src/mock/generator.ts
    - packages/shared/src/types.ts
  </read_first>
  <action>
Enrich the ServiceDetailPage for arr services to show the operational data removed from the card.

**Arr detail page layout (cockpit dot-leader format):**

```
RADARR                              ● ONLINE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
QUEUE ............................ 3 items
MONITORED ................... 1,240 movies
LIBRARY SIZE ................... 4.2 TB
MISSING .......................... 12 movies

⚠ ATTENTION REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MANUAL IMPORT ................ 2 items
  [Item 1: Movie.Title.2024.mkv]
  [Item 2: Another.Movie.mkv]
```

**Sections:**

1. **Stats section** (always shown):
   - Queue: `{n} items` (items actively in download queue)
   - Monitored: `{n} movies/shows` (depending on service)
   - Library size: `{n} TB/GB` (total managed library)
   - Missing: `{n} movies/shows` (monitored but not yet acquired)

2. **Attention Required section** (only shown when attention items exist):
   - Amber `⚠ ATTENTION REQUIRED` header bar
   - Manual import items: list of filenames needing manual import
   - Each item on its own line, amber text, truncated to fit

**Cockpit styling:**
- Use dot-leader format for all stats: `LABEL ........................ VALUE`
- Section separator: `1px solid rgba(232,160,32,0.15)`
- Attention section: amber `rgba(232,160,32,0.08)` background tint, amber left border `3px`
- Item list: smaller font (11px), off-white, monospace

**Mock generator updates:**
Add to arr service mock data:
- `queue: number` (items in queue)
- `monitored: number` (monitored count)
- `librarySize: string` (e.g. "4.2 TB")
- `missing: number`
- `attentionItems: Array<{type: 'manual_import' | 'failed', name: string}>` — set Radarr to have 2 manual import items

**ServiceDetailPage logic:**
- Detect arr services by id: `['radarr', 'sonarr', 'lidarr', 'bazarr']`
- Render arr-specific detail view for these services
- Render existing dot-leader generic view for all others
  </action>
  <verify>
    - Tapping Radarr card opens detail with queue/monitored/library stats
    - Attention section shows with 2 manual import items for Radarr
    - Other arr services show stats but no attention section (no mock items)
  </verify>
  <done>Arr detail page shows operational stats and attention items in cockpit dot-leader format.</done>
</task>

</tasks>

<verification>
Arr cards simplified on dashboard. Detail pages enriched with stats and attention items.
</verification>

<success_criteria>
- Arr card face: status LED + download state only
- Arr detail: queue count, monitored, library size, missing, attention items
- Attention section visible for Radarr (2 mock manual import items)
- Cockpit dot-leader styling throughout
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-12-SUMMARY.md`
</output>
