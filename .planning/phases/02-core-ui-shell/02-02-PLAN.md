---
phase: 02-core-ui-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/package.json
  - packages/frontend/src/styles/globals.css
  - packages/frontend/src/main.tsx
  - packages/frontend/src/App.tsx
  - packages/frontend/index.html
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/frontend/src/components/layout/GridBackground.tsx
  - packages/frontend/src/pages/DashboardPage.tsx
  - packages/frontend/src/pages/ServiceDetailPage.tsx
  - packages/frontend/src/pages/SettingsPage.tsx
  - packages/frontend/src/pages/LogsPage.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-07

must_haves:
  truths:
    - "Browser navigates between / (dashboard), /services/:serviceId, /settings, and /logs routes"
    - "Animated CSS grid background is visible behind all pages with traveling light pulses"
    - "AppHeader is fixed at top with CORUSCANT title, icon buttons, and NAS stats strip"
    - "Dashboard layout uses 2-column auto-fit grid on mobile, expands on wider screens"
    - "Monospace font applied globally via JetBrains Mono with system fallback"
  artifacts:
    - path: "packages/frontend/src/styles/globals.css"
      provides: "CSS custom properties, grid background keyframes, global styles"
      contains: "--tron-blue: #00c8ff"
    - path: "packages/frontend/src/App.tsx"
      provides: "React Router setup with all routes"
      contains: "BrowserRouter"
    - path: "packages/frontend/src/components/layout/AppHeader.tsx"
      provides: "Fixed header with title + NAS stats"
      exports: ["AppHeader"]
    - path: "packages/frontend/src/components/layout/GridBackground.tsx"
      provides: "CSS animated grid background"
      exports: ["GridBackground"]
  key_links:
    - from: "packages/frontend/src/main.tsx"
      to: "packages/frontend/src/App.tsx"
      via: "BrowserRouter wrapping App"
      pattern: "BrowserRouter"
    - from: "packages/frontend/src/App.tsx"
      to: "packages/frontend/src/pages/DashboardPage.tsx"
      via: "React Router Route path=/"
      pattern: "Route.*path.*/"
---

<objective>
Install frontend dependencies, establish the CSS design system (Tron color palette, monospace typography, animation keyframes), set up React Router with all Phase 2 routes, build the animated grid background, and create the AppHeader component.

Purpose: Creates the visual foundation and navigation skeleton that all subsequent UI components render within. After this plan, the app has a working Tron-aesthetic shell with routing.
Output: Navigable app shell with animated grid, header, and route stubs for all pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md
@.planning/phases/02-core-ui-shell/02-RESEARCH.md
@.planning/phases/02-core-ui-shell/02-UI-SPEC.md

<interfaces>
<!-- Existing frontend files to modify -->

From packages/frontend/src/main.tsx:
```typescript
import { createRoot } from 'react-dom/client'
import App from './App.js'

const rootEl = document.getElementById('root')
if (rootEl) {
  createRoot(rootEl).render(<App />)
}
```

From packages/frontend/src/App.tsx:
```typescript
function App() {
  return (
    <div style={{ fontFamily: 'monospace', padding: '2rem', color: '#00c8ff', background: '#0a0a0f', minHeight: '100vh' }}>
      <h1>Coruscant</h1>
      <p>Infrastructure monitoring hub — Phase 1 scaffold</p>
    </div>
  )
}
export default App
```

From packages/frontend/index.html:
```html
<!-- Standard Vite React template with <div id="root"> -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install frontend deps, create CSS design system, set up routing and page stubs</name>
  <files>
    packages/frontend/package.json,
    packages/frontend/index.html,
    packages/frontend/src/styles/globals.css,
    packages/frontend/src/main.tsx,
    packages/frontend/src/App.tsx,
    packages/frontend/src/pages/DashboardPage.tsx,
    packages/frontend/src/pages/ServiceDetailPage.tsx,
    packages/frontend/src/pages/SettingsPage.tsx,
    packages/frontend/src/pages/LogsPage.tsx
  </files>
  <read_first>
    packages/frontend/package.json,
    packages/frontend/src/main.tsx,
    packages/frontend/src/App.tsx,
    packages/frontend/index.html,
    .planning/phases/02-core-ui-shell/02-UI-SPEC.md,
    .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
    1. Install frontend dependencies:
    ```bash
    cd packages/frontend && npm install react-router-dom framer-motion lucide-react @emotion/is-prop-valid
    ```

    2. Update `packages/frontend/index.html` to load JetBrains Mono font (per D-12, UI-SPEC Typography):
    - Add `<link>` for Google Fonts JetBrains Mono weights 400 and 700:
      `<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">`
    - Keep existing `<div id="root">` structure

    3. Create `packages/frontend/src/styles/globals.css` with the full Tron design system:
    ```css
    /* CSS Custom Properties — Tron Design System */
    :root {
      --tron-blue: #00c8ff;
      --tron-red: #ff4444;
      --tron-amber: #ffaa00;
      --bg-dominant: #0a0a0f;
      --bg-secondary: #0d1117;
      --text-muted: #4a5568;
      --section-label: rgba(0, 200, 255, 0.45);
      --grid-line-color: rgba(0, 200, 255, 0.06);
      --grid-pulse-opacity: 0.18;
      --grid-size: 48px;
      --pulse-duration: 8s;
      --font-mono: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
    }

    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      font-family: var(--font-mono);
      font-size: 14px;
      line-height: 1.5;
      color: var(--tron-blue);
      background-color: var(--bg-dominant);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    body {
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Typography roles */
    .text-display { font-size: 20px; font-weight: 700; line-height: 1.2; letter-spacing: 0.08em; }
    .text-heading { font-size: 16px; font-weight: 700; line-height: 1.2; letter-spacing: 0.08em; }
    .text-body { font-size: 14px; font-weight: 400; line-height: 1.5; }
    .text-label { font-size: 12px; font-weight: 400; line-height: 1.4; }

    /* Animation keyframes — DASH-08: transform and opacity ONLY */

    /* Grid pulse: vertical sweep */
    @keyframes gridPulseVertical {
      0% { transform: translateY(-200px); opacity: 0; }
      10% { opacity: 1; }
      90% { opacity: 1; }
      100% { transform: translateY(100vh); opacity: 0; }
    }

    /* Grid pulse: horizontal sweep */
    @keyframes gridPulseHorizontal {
      0% { transform: translateX(-200px); opacity: 0; }
      10% { opacity: 1; }
      90% { opacity: 1; }
      100% { transform: translateX(100vw); opacity: 0; }
    }

    /* Card border trace via conic gradient angle rotation */
    @property --angle {
      syntax: '<angle>';
      inherits: false;
      initial-value: 0deg;
    }

    @keyframes borderTrace {
      to { --angle: 360deg; }
    }

    /* Health state glow animations — ONLY animate opacity */
    @keyframes breathe {
      0%, 100% { opacity: 0.3; }
      50% { opacity: 0.7; }
    }

    @keyframes pulseAmber {
      0%, 100% { opacity: 0.4; }
      50% { opacity: 0.85; }
    }

    @keyframes flashRed {
      0%, 100% { opacity: 0.2; }
      50% { opacity: 1.0; }
    }

    /* Marquee ticker for Now Playing banner */
    @keyframes marquee {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    /* Reduced motion preference — DASH-08 accessibility */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.001ms !important;
      }
    }
    ```

    4. Update `packages/frontend/src/main.tsx`:
    - Add `import './styles/globals.css'` at the top
    - Wrap `<App />` in `<BrowserRouter>` from react-router-dom:
    ```typescript
    import { createRoot } from 'react-dom/client'
    import { BrowserRouter } from 'react-router-dom'
    import App from './App.js'
    import './styles/globals.css'

    const rootEl = document.getElementById('root')
    if (rootEl) {
      createRoot(rootEl).render(
        <BrowserRouter>
          <App />
        </BrowserRouter>
      )
    }
    ```

    5. Rewrite `packages/frontend/src/App.tsx` as the route-based layout shell:
    ```tsx
    import { Routes, Route } from 'react-router-dom'
    import { AppHeader } from './components/layout/AppHeader.js'
    import { GridBackground } from './components/layout/GridBackground.js'
    import { DashboardPage } from './pages/DashboardPage.js'
    import { ServiceDetailPage } from './pages/ServiceDetailPage.js'
    import { SettingsPage } from './pages/SettingsPage.js'
    import { LogsPage } from './pages/LogsPage.js'

    export default function App() {
      return (
        <>
          <GridBackground />
          <AppHeader nas={null} />
          <main style={{ position: 'relative', zIndex: 1, paddingTop: '88px', paddingBottom: '64px' }}>
            <Routes>
              <Route path="/" element={<DashboardPage />} />
              <Route path="/services/:serviceId" element={<ServiceDetailPage />} />
              <Route path="/settings" element={<SettingsPage />} />
              <Route path="/logs" element={<LogsPage />} />
            </Routes>
          </main>
        </>
      )
    }
    ```
    Note: `nas={null}` is a placeholder — Plan 03 wires live SSE data. The `paddingTop: 88px` clears the fixed header (UI-SPEC: header total height 88px). `paddingBottom: 64px` clears the Now Playing banner + safe area.

    6. Create page stubs per D-22:

    `packages/frontend/src/pages/DashboardPage.tsx`:
    ```tsx
    export function DashboardPage() {
      return (
        <div style={{ padding: '0 16px' }}>
          <p className="text-body" style={{ color: 'var(--text-muted)' }}>
            Dashboard loading...
          </p>
        </div>
      )
    }
    ```
    (Plan 03 replaces this with CardGrid + SSE wiring)

    `packages/frontend/src/pages/ServiceDetailPage.tsx`:
    ```tsx
    import { useParams, useNavigate } from 'react-router-dom'

    export function ServiceDetailPage() {
      const { serviceId } = useParams<{ serviceId: string }>()
      const navigate = useNavigate()

      return (
        <div style={{ padding: '0 16px' }}>
          <button
            onClick={() => navigate('/')}
            style={{
              background: 'none', border: 'none', color: 'var(--tron-blue)',
              fontFamily: 'var(--font-mono)', fontSize: '14px', cursor: 'pointer',
              padding: '8px 0', marginBottom: '16px'
            }}
            aria-label="Back to dashboard"
          >
            &larr; Dashboard
          </button>
          <h1 className="text-heading" style={{ textTransform: 'capitalize', marginBottom: '16px' }}>
            {serviceId}
          </h1>
          <div style={{ color: 'var(--text-muted)' }}>
            <p className="text-body" style={{ marginBottom: '8px' }}>Status: ---</p>
            <p className="text-body" style={{ marginBottom: '8px' }}>Last checked: ---</p>
            <p className="text-body" style={{ marginBottom: '8px' }}>Response time: ---</p>
          </div>
        </div>
      )
    }
    ```

    `packages/frontend/src/pages/SettingsPage.tsx` (per D-03 — includes animation intensity stub):
    ```tsx
    export function SettingsPage() {
      return (
        <div style={{ padding: '0 16px' }}>
          <h1 className="text-heading" style={{ marginBottom: '16px' }}>Settings</h1>
          <p className="text-body" style={{ color: 'var(--text-muted)', marginBottom: '24px' }}>
            Service configuration coming in Phase 3.
          </p>
          <div style={{ marginBottom: '16px' }}>
            <label className="text-label" style={{ color: 'var(--text-muted)', display: 'block', marginBottom: '8px' }}>
              Animation Intensity
            </label>
            <input
              type="range"
              min="0"
              max="0.3"
              step="0.01"
              defaultValue="0.18"
              onChange={(e) => {
                document.documentElement.style.setProperty('--grid-pulse-opacity', e.target.value)
              }}
              style={{ width: '100%', maxWidth: '300px' }}
              aria-label="Grid animation intensity"
            />
          </div>
        </div>
      )
    }
    ```

    `packages/frontend/src/pages/LogsPage.tsx`:
    ```tsx
    export function LogsPage() {
      return (
        <div style={{ padding: '0 16px' }}>
          <h1 className="text-heading" style={{ marginBottom: '16px' }}>Logs</h1>
          <p className="text-body" style={{ color: 'var(--text-muted)' }}>
            Log viewer coming in Phase 7.
          </p>
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/frontend/package.json dependencies contains "react-router-dom"
    - packages/frontend/package.json dependencies contains "framer-motion"
    - packages/frontend/package.json dependencies contains "lucide-react"
    - packages/frontend/src/styles/globals.css contains `--tron-blue: #00c8ff`
    - packages/frontend/src/styles/globals.css contains `--tron-red: #ff4444`
    - packages/frontend/src/styles/globals.css contains `--tron-amber: #ffaa00`
    - packages/frontend/src/styles/globals.css contains `--bg-dominant: #0a0a0f`
    - packages/frontend/src/styles/globals.css contains `@keyframes breathe`
    - packages/frontend/src/styles/globals.css contains `@keyframes borderTrace`
    - packages/frontend/src/styles/globals.css contains `@media (prefers-reduced-motion: reduce)`
    - packages/frontend/src/main.tsx contains `BrowserRouter`
    - packages/frontend/src/main.tsx contains `import './styles/globals.css'`
    - packages/frontend/src/App.tsx contains `Route path="/"`
    - packages/frontend/src/App.tsx contains `Route path="/services/:serviceId"`
    - packages/frontend/src/App.tsx contains `Route path="/settings"`
    - packages/frontend/src/App.tsx contains `Route path="/logs"`
    - packages/frontend/src/pages/SettingsPage.tsx contains `Animation Intensity`
    - packages/frontend/src/pages/SettingsPage.tsx contains `--grid-pulse-opacity`
    - packages/frontend/src/pages/LogsPage.tsx contains `Log viewer coming in Phase 7`
    - `npm run build --workspace=packages/frontend` exits 0
  </acceptance_criteria>
  <done>Frontend dependencies installed. CSS design system with Tron color palette, typography scale, and all animation keyframes created. React Router configured with 4 routes (/, /services/:id, /settings, /logs). Page stubs render with correct copy. Settings stub includes animation intensity slider (D-03). Vite build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build GridBackground and AppHeader components</name>
  <files>
    packages/frontend/src/components/layout/GridBackground.tsx,
    packages/frontend/src/components/layout/AppHeader.tsx
  </files>
  <read_first>
    packages/frontend/src/styles/globals.css,
    packages/frontend/src/App.tsx,
    .planning/phases/02-core-ui-shell/02-UI-SPEC.md
  </read_first>
  <action>
    1. Create `packages/frontend/src/components/layout/GridBackground.tsx` (per D-01, D-02, UI-SPEC Animation Contract):
    ```tsx
    export function GridBackground() {
      return (
        <div
          aria-hidden="true"
          style={{
            position: 'fixed',
            inset: 0,
            zIndex: 0,
            backgroundColor: 'var(--bg-dominant)',
            backgroundImage: `
              linear-gradient(var(--grid-line-color) 1px, transparent 1px),
              linear-gradient(90deg, var(--grid-line-color) 1px, transparent 1px)
            `,
            backgroundSize: 'var(--grid-size) var(--grid-size)',
            pointerEvents: 'none',
          }}
        >
          {/* Vertical pulse sweep */}
          <div
            style={{
              position: 'absolute',
              inset: 0,
              background: `linear-gradient(to bottom, transparent 0%, rgba(0, 200, 255, var(--grid-pulse-opacity)) 50%, transparent 100%)`,
              backgroundSize: '100% 200px',
              animation: 'gridPulseVertical var(--pulse-duration) linear infinite',
              willChange: 'transform',
              opacity: 1,
            }}
          />
          {/* Horizontal pulse sweep */}
          <div
            style={{
              position: 'absolute',
              inset: 0,
              background: `linear-gradient(to right, transparent 0%, rgba(0, 200, 255, calc(var(--grid-pulse-opacity) * 0.6)) 50%, transparent 100%)`,
              backgroundSize: '200px 100%',
              animation: 'gridPulseHorizontal calc(var(--pulse-duration) * 1.3) linear infinite',
              animationDelay: 'calc(var(--pulse-duration) * 0.5)',
              willChange: 'transform',
              opacity: 1,
            }}
          />
        </div>
      )
    }
    ```
    Key points:
    - `aria-hidden="true"` — decorative only
    - `pointerEvents: 'none'` — grid never intercepts clicks (D-02)
    - `position: fixed` — stays put as user scrolls (D-02)
    - Two pulse sweeps on separate pseudo-elements: vertical (8s) and horizontal (10.4s offset) for organic feel
    - Uses `--grid-pulse-opacity` CSS variable that Settings slider controls (D-03)

    2. Create `packages/frontend/src/components/layout/AppHeader.tsx` (per D-05, D-06, D-07, UI-SPEC Layout):
    ```tsx
    import { useNavigate } from 'react-router-dom'
    import { Settings, List } from 'lucide-react'
    import type { NasStatus } from '@coruscant/shared'

    interface AppHeaderProps {
      nas: NasStatus | null
    }

    export function AppHeader({ nas }: AppHeaderProps) {
      const navigate = useNavigate()

      return (
        <header
          style={{
            position: 'fixed',
            top: 0,
            left: 0,
            right: 0,
            zIndex: 10,
            background: 'rgba(13, 17, 23, 0.92)',
            backdropFilter: 'blur(8px)',
            borderBottom: '1px solid rgba(0, 200, 255, 0.1)',
          }}
        >
          {/* Title row — 44px height per UI-SPEC */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              height: '44px',
              padding: '0 16px',
            }}
          >
            <span className="text-display" style={{ color: 'var(--tron-blue)' }}>
              CORUSCANT
            </span>
            <div style={{ display: 'flex', gap: '4px' }}>
              <button
                onClick={() => navigate('/settings')}
                aria-label="Open Settings"
                style={{
                  background: 'none',
                  border: 'none',
                  color: 'var(--tron-blue)',
                  cursor: 'pointer',
                  width: '44px',
                  height: '44px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <Settings size={24} />
              </button>
              <button
                onClick={() => navigate('/logs')}
                aria-label="Open Logs"
                style={{
                  background: 'none',
                  border: 'none',
                  color: 'var(--tron-blue)',
                  cursor: 'pointer',
                  width: '44px',
                  height: '44px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <List size={24} />
              </button>
            </div>
          </div>

          {/* NAS stats strip — 44px height per UI-SPEC D-05 */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              height: '44px',
              padding: '0 16px',
              borderTop: '1px solid rgba(0, 200, 255, 0.06)',
            }}
          >
            <div style={{ display: 'flex', gap: '16px' }}>
              <span>
                <span className="text-label" style={{ color: 'var(--text-muted)' }}>CPU </span>
                <span className="text-body" style={{ color: 'var(--tron-blue)' }}>
                  {nas ? `${Math.round(nas.cpu)}%` : '---'}
                </span>
              </span>
              <span>
                <span className="text-label" style={{ color: 'var(--text-muted)' }}>RAM </span>
                <span className="text-body" style={{ color: 'var(--tron-blue)' }}>
                  {nas ? `${Math.round(nas.ram)}%` : '---'}
                </span>
              </span>
            </div>
            <div style={{ display: 'flex', gap: '16px' }}>
              {nas?.volumes.map((vol) => (
                <span key={vol.name}>
                  <span className="text-label" style={{ color: 'var(--text-muted)' }}>{vol.name} </span>
                  <span className="text-body" style={{ color: 'var(--tron-blue)' }}>
                    {Math.round(vol.usedPercent)}%
                  </span>
                  {vol.tempC != null && (
                    <span className="text-body" style={{ color: 'var(--tron-blue)', marginLeft: '8px' }}>
                      {vol.tempC}&deg;C
                    </span>
                  )}
                </span>
              )) ?? (
                <span className="text-body" style={{ color: 'var(--text-muted)' }}>---</span>
              )}
            </div>
          </div>
        </header>
      )
    }
    ```
    Key points:
    - Two 44px rows = 88px total (UI-SPEC AppHeader dimensions)
    - Icon buttons are 44x44 touch targets (iOS HIG, UI-SPEC Accessibility)
    - Uses lucide-react Settings and List icons (UI-SPEC Component Inventory)
    - `nas` prop is `NasStatus | null` — renders "---" when null (mock data wired in Plan 03)
    - Semi-transparent background with blur for Tron glow effect (D-06)
    - `aria-label` on both icon buttons (UI-SPEC Accessibility)
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/frontend/src/components/layout/GridBackground.tsx contains `position: 'fixed'`
    - packages/frontend/src/components/layout/GridBackground.tsx contains `aria-hidden="true"`
    - packages/frontend/src/components/layout/GridBackground.tsx contains `pointerEvents: 'none'`
    - packages/frontend/src/components/layout/GridBackground.tsx contains `gridPulseVertical`
    - packages/frontend/src/components/layout/GridBackground.tsx contains `gridPulseHorizontal`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `export function AppHeader`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `CORUSCANT`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `aria-label="Open Settings"`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `aria-label="Open Logs"`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `NasStatus`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `width: '44px'`
    - packages/frontend/src/components/layout/AppHeader.tsx contains `height: '44px'`
    - `npm run build --workspace=packages/frontend` exits 0
  </acceptance_criteria>
  <done>GridBackground renders fixed animated grid with two pulse sweeps (vertical + horizontal) using CSS keyframes. AppHeader renders fixed 88px header with CORUSCANT title, Settings/Logs icon buttons (44x44 touch targets), and NAS stats strip accepting NasStatus prop. Vite build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/frontend` — build succeeds with 0 errors
- Browser at localhost:5173 (vite dev) shows animated grid background with traveling pulses
- Navigating to `/settings`, `/logs`, `/services/radarr` via address bar renders correct stubs
- Header shows CORUSCANT with icon buttons that navigate to /settings and /logs
</verification>

<success_criteria>
- Tron design system CSS custom properties established (colors, spacing, typography, animations)
- React Router configured with all 4 route patterns from D-22
- Animated grid background visible with traveling light pulses (DASH-02)
- AppHeader fixed at top with correct layout (D-05, D-06, D-07)
- All routes navigable; stub pages render correct copy (per UI-SPEC Copywriting)
- Settings stub includes animation intensity slider (D-03)
- Vite build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-02-SUMMARY.md`
</output>
