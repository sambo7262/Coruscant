---
phase: 02-core-ui-shell
plan: 10
type: execute
wave: 4
depends_on:
  - "02-06"
  - "02-07"
  - "02-08"
  - "02-09"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "Dashboard renders correctly on 800x480 viewport with cockpit aesthetic"
    - "All service cards show instrument-specific bodies"
    - "LEDs animate per health state"
    - "Back navigation works from detail/settings/logs pages"
    - "No Tron/cyan colors visible anywhere"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint — validate the complete Star Wars cockpit retheme on an 800x480 viewport.

Purpose: Confirm the aesthetic rework meets user expectations before closing the phase. Human eyes verify what automated tests cannot.
Output: User approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and start dev server for visual inspection</name>
  <files></files>
  <read_first>
    - packages/frontend/package.json
    - packages/backend/package.json
  </read_first>
  <action>
Run the following to prepare for visual verification:

1. `cd /Users/Oreo/Projects/Coruscant && npx tsc --build` — verify full project compiles
2. `cd packages/frontend && npx tsc --noEmit` — verify frontend types
3. `grep -rc "tron\|#00c8ff\|#ff4444\|#ffaa00\|rgba(0, 200, 255" packages/frontend/src/` — confirm ZERO Tron color references remain in entire frontend source
4. `grep -rc "tron\|#00c8ff" packages/backend/src/` — confirm backend has no Tron refs (mock generator clean)

Report any remaining Tron references found. If zero, the codebase is clean for visual review.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -rc "tron\|#00c8ff\|rgba(0, 200, 255" packages/frontend/src/ | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compilation passes with no errors
    - grep for Tron colors across entire frontend/src returns 0 matches in all files
    - No file contains "#00c8ff", "var(--tron-", or "rgba(0, 200, 255"
  </acceptance_criteria>
  <done>Full project compiles. Zero Tron color references remain in frontend or backend source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on 800x480 viewport</name>
  <action>
Human visually verifies the complete Star Wars cockpit retheme on an 800x480 viewport. This task is a checkpoint — no code changes, only visual inspection and approval.
  </action>
  <what-built>
Complete Star Wars X-Wing cockpit aesthetic retheme of the Coruscant dashboard:
- Amber/green/red cockpit palette replacing Tron cyan/blue
- Static instrument wall background with amber seam lines (no animated grid)
- Decorative SVG wiring traces behind cards
- CRT scanline overlay for texture depth
- Chamfered instrument panel cards with service-specific bodies (NAS gauges, arr dot matrix, Plex signal bars, SABnzbd progress, Pi-hole stats)
- 10px round LED status indicators with health-state animations
- Cockpit chrome AppHeader with amber palette
- Back navigation (arrow + CORUSCANT link) on sub-pages
- Utilitarian Now Playing banner with USER > TITLE stream format
- Dot-leader readout format on service detail pages
- 800x480 optimized grid layout (4 cards per row)
  </what-built>
  <how-to-verify>
1. Start the dev servers: `cd /Users/Oreo/Projects/Coruscant && npm run dev` (or appropriate dev command)
2. Open browser to http://localhost:5173 (or the Vite dev server port)
3. Set browser viewport to exactly 800x480 (Chrome DevTools -> Toggle device toolbar -> set dimensions)

**Dashboard page (/):**
- [ ] Background: dark panels with faint amber seam lines, no animated pulses
- [ ] SVG wiring traces faintly visible behind cards at ~8% amber opacity
- [ ] CRT scanline texture visible (subtle horizontal lines across screen)
- [ ] AppHeader: CORUSCANT title in amber, NAS stats in amber, Settings/Logs icons in amber
- [ ] Section labels (STATUS, ACTIVITY, RICH DATA) in amber uppercase
- [ ] Cards: chamfered corners, amber header strip, LED status dots
- [ ] NAS card: horizontal bar gauges for CPU/RAM/Disk
- [ ] Arr cards (Radarr/Sonarr/Lidarr/Bazarr): dot matrix LED grid
- [ ] Plex card: signal strength bars
- [ ] SABnzbd card: progress bar with speed label
- [ ] Pi-hole card: stat readouts (blocked %, queries)
- [ ] LED colors: green=online, amber=warning, red=offline, grey=stale
- [ ] LED animations: slow breathe (green), faster pulse (amber), rapid flash (red), static (grey)
- [ ] Card entrance stagger animation on page load
- [ ] ~4 cards per row at 800px width
- [ ] No cyan, no neon blue anywhere
- [ ] Now Playing banner at bottom with amber "N streams" label

**Card tap -> Detail page (/services/:id):**
- [ ] AppHeader shows "back arrow CORUSCANT" back link in amber
- [ ] Settings/Logs buttons hidden on detail page
- [ ] Service name in amber, status LED
- [ ] Metric readouts in dot-leader format: LABEL ........... VALUE
- [ ] Tapping back arrow returns to dashboard

**Settings page (/settings):**
- [ ] AppHeader shows back arrow CORUSCANT back link
- [ ] LED intensity slider visible
- [ ] Text in cockpit palette (amber headings, off-white body)

**Logs page (/logs):**
- [ ] AppHeader shows back arrow CORUSCANT back link
- [ ] Text in cockpit palette

**Now Playing banner:**
- [ ] Tap to expand: amber-bordered drawer slides up
- [ ] Stream rows: USER > TITLE format left, QUAL / DIRECT right
- [ ] 1px amber progress bar below each stream row
- [ ] Tap outside or tap again to collapse

**Overall feel:**
- [ ] Feels like a Star Wars X-Wing cockpit instrument panel — warm amber on near-black, utilitarian, physical
- [ ] NOT Tron — no cyan, no neon, no animated grid pulses, no border traces
  </how-to-verify>
  <resume-signal>Type "approved" if the cockpit aesthetic looks correct, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
User visual approval on 800x480 viewport confirms the retheme is complete.
</verification>

<success_criteria>
- Zero Tron references in codebase (automated check passes)
- User approves the visual appearance on 800x480 viewport
- All interactive flows work (card tap, back nav, banner expand/collapse)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-10-SUMMARY.md`
</output>
