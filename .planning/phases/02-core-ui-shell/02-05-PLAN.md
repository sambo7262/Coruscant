---
phase: 02-core-ui-shell
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "Dashboard renders correctly on a phone-width viewport with animated grid, cards, and banner"
    - "All card animations run at 60fps without jank on mobile Chrome"
    - "Now Playing banner expands/collapses smoothly"
    - "Card tap navigates to detail; back returns to dashboard at correct scroll position"
    - "NAS stats update in header every 5 seconds from SSE"
  artifacts: []
  key_links: []
---

<objective>
Run the full application end-to-end and visually verify all Phase 2 requirements on a mobile viewport. This is a human verification checkpoint — no code changes, just testing the integrated result.

Purpose: Confirms the complete Phase 2 UI experience works as intended before marking the phase complete. Catches visual/animation issues that automated tests cannot detect.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md
@.planning/phases/02-core-ui-shell/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete Phase 2 UI on mobile viewport</name>
  <files></files>
  <action>
    Start both backend and frontend dev servers, then present the verification checklist to the user.

    Terminal 1: `cd /Users/Oreo/Projects/Coruscant && npm run dev --workspace=packages/backend`
    Terminal 2: `cd /Users/Oreo/Projects/Coruscant && npm run dev --workspace=packages/frontend`

    Open http://localhost:5173 in Chrome with mobile device emulation (iPhone 14 Pro, 393x852).

    Complete Phase 2 Tron/Grid UI shell includes:
    - Animated CSS grid background with traveling light pulses
    - AppHeader with CORUSCANT title, NAS stats strip (CPU/RAM/disk updating via SSE), Settings and Logs icon buttons
    - Service card grid grouped by tier (STATUS, ACTIVITY, RICH DATA) with animated border traces and health-state glow
    - Now Playing banner at bottom with expand/collapse drawer showing 2 mock Plex streams
    - Card tap navigation to detail view with mock metric slots
    - Settings stub with animation intensity slider
    - Full SSE data pipeline: mock data generator -> GET /api/sse -> EventSource -> React state -> animated UI
  </action>
  <verify>
    User visually confirms all Phase 2 success criteria:

    **SC-1: Animated grid background (DASH-02)**
    - [ ] Grid lines visible on dark background
    - [ ] Faint blue pulse sweeps across the grid (vertical and horizontal)
    - [ ] Grid stays fixed when scrolling — cards move over it

    **SC-2: Card border traces and health glow (DASH-03, DASH-04, DASH-05)**
    - [ ] Card borders have animated light traces (conic gradient sweep)
    - [ ] Each card trace is offset from neighbors (staggered, not synchronized)
    - [ ] Online cards glow Tron Blue (#00c8ff) with slow breathing
    - [ ] Warning card (lidarr) glows Amber (#ffaa00) with faster pulse
    - [ ] Offline card (bazarr) glows Red (#ff4444) with sharp flash

    **SC-3: Card tap navigation (DASH-07)**
    - [ ] Tap any service card — navigates to /services/{id} detail page
    - [ ] Detail page shows service name with status dot and mock metric slots
    - [ ] Browser back returns to dashboard
    - [ ] Scroll position is preserved after returning

    **SC-4: Now Playing banner (DASH-06)**
    - [ ] Banner visible at bottom with stream count and ticker
    - [ ] Tap banner — drawer slides up showing both mock streams
    - [ ] Stream rows show: title, user, progress bar, quality, TRANSCODE/DIRECT label
    - [ ] Tap banner again or tap backdrop — drawer collapses smoothly

    **SC-5: Responsive layout (DASH-01)**
    - [ ] 2-column card grid on phone (393px width)
    - [ ] Switch to desktop viewport — grid expands to 3+ columns
    - [ ] No horizontal scrolling on either viewport

    **SC-6: Performance (DASH-08)**
    - [ ] Record 5 seconds of idle animation in Chrome DevTools Performance tab
    - [ ] Frame rate stays at or near 60fps

    **SC-7: SSE data pipeline**
    - [ ] NAS stats in header update every ~5 seconds (CPU value jitters)
    - [ ] EventSource connection to /api/sse visible in Network tab

    **SC-8: Settings and Logs stubs**
    - [ ] Settings icon navigates to /settings with animation intensity slider
    - [ ] Logs icon navigates to /logs with stub text
  </verify>
  <done>User types "approved" confirming all Phase 2 visual and functional requirements are met on mobile and desktop viewports.</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
All 8 DASH requirements verified visually on mobile viewport.
</verification>

<success_criteria>
User confirms all Phase 2 success criteria from ROADMAP.md are met:
1. Animated grid background with traveling light pulses
2. Service card border traces and health-state glow with Tron color system
3. Card tap navigation to detail view; browser back returns
4. Now Playing banner renders and scrolls at 60fps
5. Dashboard layout usable on both phone and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-05-SUMMARY.md`
</output>
