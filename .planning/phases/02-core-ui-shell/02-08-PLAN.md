---
phase: 02-core-ui-shell
plan: 08
type: execute
wave: 2
depends_on:
  - "02-06"
files_modified:
  - packages/frontend/src/components/layout/GridBackground.tsx
  - packages/frontend/src/components/cards/ServiceCard.tsx
  - packages/frontend/src/components/cards/CardGrid.tsx
  - packages/backend/src/mock/generator.ts
autonomous: true
gap_closure: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-01

must_haves:
  truths:
    - "Background shows dark structural panel segments with amber seam lines — no animated grid pulses"
    - "Service cards are chamfered instrument panels with amber header strip, LED status indicator, and service-specific instrument body"
    - "Card grid uses minmax(180px, 1fr) for 800x480 viewport, section labels in amber"
    - "NAS card shows horizontal bar gauges, Plex shows signal bars, SABnzbd shows progress bar, Pi-hole shows stat readouts"
  artifacts:
    - path: "packages/frontend/src/components/layout/GridBackground.tsx"
      provides: "Static instrument wall panel background"
      contains: "--bg-seam"
    - path: "packages/frontend/src/components/cards/ServiceCard.tsx"
      provides: "Instrument cluster card with service-specific body"
      contains: "chamfer-card"
    - path: "packages/frontend/src/components/cards/CardGrid.tsx"
      provides: "Grid layout optimized for 800x480"
      contains: "minmax(180px"
  key_links:
    - from: "packages/frontend/src/components/cards/ServiceCard.tsx"
      to: "packages/frontend/src/styles/globals.css"
      via: "CSS custom properties and chamfer-card class"
      pattern: "chamfer-card"
    - from: "packages/frontend/src/components/cards/ServiceCard.tsx"
      to: "packages/frontend/src/components/ui/StatusDot.tsx"
      via: "import StatusDot"
      pattern: "StatusDot"
---

<objective>
Replace the animated grid background with a static instrument wall panel, rebuild ServiceCard as a service-specific instrument cluster, and update CardGrid for the 800x480 viewport.

Purpose: These are the core visual surfaces — background, cards, and grid. After this plan, the dashboard looks like an X-Wing instrument panel rather than a Tron interface.
Output: Static structural background, instrument-specific cards, 800x480-optimized grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md

@packages/frontend/src/components/layout/GridBackground.tsx
@packages/frontend/src/components/cards/ServiceCard.tsx
@packages/frontend/src/components/cards/CardGrid.tsx
@packages/shared/src/types.ts
@packages/backend/src/mock/generator.ts

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface ServiceStatus {
  id: string
  name: string
  tier: 'status' | 'activity' | 'rich'
  status: 'online' | 'offline' | 'warning' | 'stale'
  lastPollAt: string
  metrics?: Record<string, unknown>
}

export interface NasStatus {
  cpu: number
  ram: number
  volumes: { name: string; usedPercent: number; tempC?: number }[]
}

export interface DashboardSnapshot {
  services: ServiceStatus[]
  nas: NasStatus
  streams: PlexStream[]
  timestamp: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace animated grid with static instrument wall panel + update mock generator</name>
  <files>packages/frontend/src/components/layout/GridBackground.tsx, packages/backend/src/mock/generator.ts</files>
  <read_first>
    - packages/frontend/src/components/layout/GridBackground.tsx
    - packages/backend/src/mock/generator.ts
    - packages/shared/src/types.ts
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
**GridBackground.tsx — complete rewrite (per D-08, D-11):**

Replace the animated light grid with a static instrument wall panel. The background is a dark surface divided by visible amber seam lines forming panel "slots".

Implementation:
- Keep `position: fixed`, `inset: 0`, `z-index: 0`, `pointer-events: none`, `aria-hidden="true"`
- Background color: `#0A0A0A` (body background)
- Render structural seam lines using CSS `background-image` with `linear-gradient`:
  - Horizontal seams: `repeating-linear-gradient(to bottom, transparent 0px, transparent 119px, rgba(232, 160, 32, 0.15) 119px, rgba(232, 160, 32, 0.15) 120px)` — 1px amber line every 120px
  - Vertical seams: `repeating-linear-gradient(to right, transparent 0px, transparent 199px, rgba(232, 160, 32, 0.15) 199px, rgba(232, 160, 32, 0.15) 200px)` — 1px amber line every 200px
- Use `background-image` with both gradients comma-separated
- NO animation whatsoever — this is a static structural background
- Remove ALL references to `gridPulseVertical`, `gridPulseHorizontal`, `--grid-pulse-opacity`, `--pulse-duration`, `--grid-line-color`, `--grid-size`
- Remove the two animated child divs (vertical pulse sweep, horizontal pulse sweep)

**Mock generator update:**
Add `metrics` fields to mock services so ServiceCard instrument bodies have data to display:

```typescript
// NAS detail card — gauge data
{ id: 'nas-detail', name: 'NAS', tier: 'rich', status: 'stale', lastPollAt: staleTime,
  metrics: { cpu: nas.cpu, ram: nas.ram, diskPercent: 67, tempC: 42 } }

// Plex — signal/stream data
{ id: 'plex', name: 'Plex', tier: 'rich', status: 'online', lastPollAt: nowIso,
  metrics: { activeStreams: 2, maxStreams: 5 } }

// SABnzbd — download data
{ id: 'sabnzbd', name: 'SABnzbd', tier: 'activity', status: 'online', lastPollAt: nowIso,
  metrics: { speedMBs: 12.4, queueCount: 3, progressPercent: 65 } }

// Pi-hole — stat data
{ id: 'pihole', name: 'Pi-hole', tier: 'rich', status: 'online', lastPollAt: nowIso,
  metrics: { blockedPercent: 34.2, totalQueries: 48291 } }

// Radarr/Sonarr/Lidarr/Bazarr — queue item counts for dot matrix
{ id: 'radarr', ..., metrics: { queueCount: 5, monitoredCount: 12 } }
{ id: 'sonarr', ..., metrics: { queueCount: 8, monitoredCount: 20 } }
{ id: 'lidarr', ..., metrics: { queueCount: 2, monitoredCount: 6 } }
{ id: 'bazarr', ..., metrics: { queueCount: 0, monitoredCount: 4 } }
```

Keep all existing service entries — just add/update the `metrics` field on each. Do NOT change the ServiceStatus type (metrics is already `Record<string, unknown>`).
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "gridPulse" packages/frontend/src/components/layout/GridBackground.tsx; grep -q "rgba(232, 160, 32, 0.15)" packages/frontend/src/components/layout/GridBackground.tsx && echo "PASS: amber seam lines" || echo "FAIL"; grep -c "animation" packages/frontend/src/components/layout/GridBackground.tsx; grep -q "metrics" packages/backend/src/mock/generator.ts && echo "PASS: metrics in mock" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "gridPulse" in GridBackground.tsx returns 0 matches
    - grep for "animation" in GridBackground.tsx returns 0 matches
    - grep for "willChange" in GridBackground.tsx returns 0 matches
    - grep for "rgba(232, 160, 32, 0.15)" in GridBackground.tsx returns a match (seam line color)
    - grep for "repeating-linear-gradient" in GridBackground.tsx returns a match
    - GridBackground has no animated child divs — only a single container div
    - Mock generator has "metrics" on at least 6 service entries
    - grep for "activeStreams" in generator.ts returns a match (Plex metrics)
    - grep for "blockedPercent" in generator.ts returns a match (Pi-hole metrics)
    - grep for "speedMBs" in generator.ts returns a match (SABnzbd metrics)
  </acceptance_criteria>
  <done>GridBackground is a static instrument wall with amber seam lines, no animations. Mock generator provides metrics data for all service-specific instrument bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ServiceCard as instrument cluster with service-specific body</name>
  <files>packages/frontend/src/components/cards/ServiceCard.tsx, packages/frontend/src/components/cards/CardGrid.tsx</files>
  <read_first>
    - packages/frontend/src/components/cards/ServiceCard.tsx
    - packages/frontend/src/components/cards/CardGrid.tsx
    - packages/frontend/src/components/ui/StatusDot.tsx
    - packages/shared/src/types.ts
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
**ServiceCard.tsx — complete rewrite (per D-15 through D-20):**

Replace the Tron-styled card with a chamfered instrument panel.

**Card outer container:**
- Use `className="chamfer-card"` for chamfered corners (clip-path from globals.css)
- Background: `var(--bg-panel)` (#0D0D0D)
- Border: `1px solid var(--border-rest)` (rgba(232,160,32,0.20))
- On hover: border color `var(--border-hover)` (rgba(232,160,32,0.60)) — use inline style with onMouseEnter/onMouseLeave state
- Inner shadow: `box-shadow: inset 0 1px 0 rgba(255,255,255,0.04), inset 0 -1px 0 rgba(0,0,0,0.4)`
- Non-healthy card outline glow (per D-13): `warning` gets `box-shadow: 0 0 12px rgba(232,160,32,0.3)` added, `offline` gets `box-shadow: 0 0 12px rgba(255,59,59,0.3)` added (append to inset shadow)
- Remove ALL: conic-gradient, borderTrace, `--angle`, `var(--tron-*)` references
- Height: `auto` with min-height `160px` (per D-26 — cards ~160px tall for instrument content)
- Padding: `0` (header strip + body handle their own padding)
- Keep Framer Motion entrance: `initial={{ opacity: 0, y: 12 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ duration: 0.3, ease: 'easeOut', delay: index * 0.05 }}` (per D-20)
- Keep `whileTap={{ scale: 0.97 }}`, onClick, role="button", tabIndex, aria-label, onKeyDown
- Keep sessionStorage scroll save on click

**Card header strip (per D-17):**
- 6px tall amber bar: `height: 6px`, `background: var(--cockpit-amber)`, full width at top of card
- Below the bar: a row with `padding: 8px 12px 0 12px`
  - Service name: uppercase, `text-label` class, `color: var(--cockpit-amber)`, `text-transform: uppercase`, `font-weight: 600`
  - StatusDot flush right (flexbox justify-content: space-between)

**Card body — service-specific instrument (per D-18):**
Create a helper function `renderInstrumentBody(service: ServiceStatus)` that switches on `service.id`:

**NAS (`id === 'nas-detail'`)** — horizontal bar gauges:
- Render 3-4 labeled meter bars for CPU, RAM, Disk, Temp
- Each gauge: label in `text-label` off-white left, percentage right in amber, bar below
- Bar: height 4px, background `rgba(232,160,32,0.15)`, fill with `var(--cockpit-amber)` at the percentage width
- Use `metrics.cpu`, `metrics.ram`, `metrics.diskPercent`, `metrics.tempC` from service.metrics

**Radarr/Sonarr/Lidarr/Bazarr (`id` is one of those)** — dot matrix:
- Render a grid of small square "LEDs": 8px wide, 8px tall, 2px gap
- Grid dimensions: 4 columns x 3 rows = 12 slots (Claude's discretion)
- Fill slots up to `metrics.monitoredCount` (or 12, whichever is smaller)
- Color: `var(--cockpit-green)` for healthy slots, `var(--cockpit-amber)` for queue items (first N = queueCount), `var(--cockpit-grey)` for empty slots
- Wrap in a container with `display: grid; grid-template-columns: repeat(4, 8px); gap: 2px`

**Plex (`id === 'plex'`)** — signal strength bars:
- 5 vertical bars side by side, heights staggered: 4px, 8px, 12px, 16px, 20px
- Width 6px each, gap 3px
- Fill bars up to `metrics.activeStreams` count with `var(--cockpit-green)`, rest `rgba(232,160,32,0.15)`
- Below bars: `text-label` with `{activeStreams} ACTIVE` in off-white

**SABnzbd (`id === 'sabnzbd'`)** — progress bar:
- Single progress bar: height 6px, background `rgba(232,160,32,0.15)`, fill `var(--cockpit-amber)` at `metrics.progressPercent`%
- Above bar: `text-label` with speed: `{speedMBs} MB/s` in amber, queue count: `{queueCount} queued` in off-white

**Pi-hole (`id === 'pihole'`)** — stat readouts:
- Two lines: `BLOCKED {blockedPercent}%` and `QUERIES {totalQueries}`
- Use `text-label` class, labels in off-white, values in amber

**Generic/other** — LED + name only:
- Just the header strip + StatusDot. No body content below.

Wrap all body content in a `<div style={{ padding: '8px 12px 12px 12px' }}>`.

If `service.metrics` is undefined/null, fall back to generic display for all service types.

**CardGrid.tsx — restyle (per D-25, D-26, D-27):**
- Section labels: color `var(--section-label)` which is `var(--cockpit-amber)` (amber, per D-25). Keep `text-transform: uppercase`, `text-heading` class.
- Grid: change `minmax(160px, 1fr)` to `minmax(180px, 1fr)` (per D-26 for 800px viewport = ~4 cards/row)
- Gap: keep 16px
- Skeleton cards: update border color from cyan to `var(--border-rest)`, background to `var(--bg-panel)`, add `className="chamfer-card"`, height to `160px`
- Remove all `rgba(0, 200, 255, ...)` color references
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tron" packages/frontend/src/components/cards/ServiceCard.tsx; grep -c "conic-gradient" packages/frontend/src/components/cards/ServiceCard.tsx; grep -q "chamfer-card" packages/frontend/src/components/cards/ServiceCard.tsx && echo "PASS: chamfer" || echo "FAIL"; grep -q "renderInstrumentBody\|instrument" packages/frontend/src/components/cards/ServiceCard.tsx && echo "PASS: instrument body" || echo "FAIL"; grep -q "minmax(180px" packages/frontend/src/components/cards/CardGrid.tsx && echo "PASS: 180px grid" || echo "FAIL"; grep -c "rgba(0, 200, 255" packages/frontend/src/components/cards/CardGrid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for "tron" in ServiceCard.tsx returns 0 matches
    - grep for "conic-gradient" in ServiceCard.tsx returns 0 matches
    - grep for "borderTrace" in ServiceCard.tsx returns 0 matches
    - grep for "--angle" in ServiceCard.tsx returns 0 matches
    - grep for "chamfer-card" in ServiceCard.tsx returns a match
    - ServiceCard contains instrument body rendering for at least: "nas-detail", "plex", "sabnzbd", "pihole"
    - ServiceCard contains dot matrix rendering for arr services (grep for grid-template-columns.*8px or similar)
    - grep for "var(--cockpit-amber)" in ServiceCard.tsx returns a match
    - grep for "var(--bg-panel)" in ServiceCard.tsx returns a match
    - CardGrid uses "minmax(180px, 1fr)"
    - grep for "rgba(0, 200, 255" in CardGrid.tsx returns 0 matches
    - grep for "section-label" or "cockpit-amber" in CardGrid.tsx returns a match
  </acceptance_criteria>
  <done>ServiceCard renders as chamfered instrument panel with service-specific body (gauges, dot matrix, signal bars, progress bar, stat readouts). CardGrid uses 180px minimum columns for 800x480 viewport. Zero Tron references remain.</done>
</task>

</tasks>

<verification>
- `grep -rc "tron" packages/frontend/src/components/cards/ packages/frontend/src/components/layout/GridBackground.tsx` returns all zeros
- `grep -c "conic-gradient" packages/frontend/src/components/cards/ServiceCard.tsx` returns 0
- `grep "chamfer-card" packages/frontend/src/components/cards/ServiceCard.tsx` returns match
- `cd packages/frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
- GridBackground is a static structural panel with amber seam lines and no animations
- ServiceCard is a chamfered instrument panel with 6px amber header strip and service-specific body
- All 6 service instrument types implemented (NAS gauges, arr dot matrix, Plex signal bars, SABnzbd progress, Pi-hole stats, generic LED)
- CardGrid uses 180px minimum columns for 800x480
- Mock generator provides metrics for instrument bodies
- Zero Tron/cyan references in any modified file
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-08-SUMMARY.md`
</output>
