---
phase: 02-core-ui-shell
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/backend/src/mock/generator.ts
  - packages/frontend/src/components/cards/CardGrid.tsx
autonomous: true
gap_closure: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-04

must_haves:
  truths:
    - "AppHeader middle section shows NAS instrument panel — vertical bar gauges for CPU/RAM/disk, drive temp bars in °F"
    - "NAS volume label reads 'TheRock' not 'vol1' or generic name"
    - "All NAS temperatures displayed in Fahrenheit"
    - "Standalone NAS card removed from the card grid"
    - "Header remains single-row and fits 800px width without overflow"
  artifacts:
    - path: "packages/frontend/src/components/layout/AppHeader.tsx"
      provides: "AppHeader with NAS instrument panel in middle section"
      contains: "TheRock"
    - path: "packages/backend/src/mock/generator.ts"
      provides: "Mock NAS data with Fahrenheit temps and TheRock label"
      contains: "TheRock"
  key_links: []
---

<objective>
Move NAS stats from a standalone card into the AppHeader's unused middle section as a compact instrument panel. The header has CORUSCANT title on the left and Settings/Logs icons on the right — the wide middle is empty. Fill it with live NAS readouts: small vertical bar gauges for CPU, RAM, and disk usage, plus drive temperature bars in °F. Remove the NAS card from CardGrid entirely to eliminate duplication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NAS instrument panel to AppHeader middle section</name>
  <files>
    packages/frontend/src/components/layout/AppHeader.tsx
    packages/frontend/src/styles/globals.css
    packages/backend/src/mock/generator.ts
  </files>
  <read_first>
    - packages/frontend/src/components/layout/AppHeader.tsx
    - packages/frontend/src/styles/globals.css
    - packages/backend/src/mock/generator.ts
    - packages/shared/src/types.ts
  </read_first>
  <action>
Expand AppHeader to render NAS stats inline in the header's middle section.

**Header layout (800px, single row):**
```
[CORUSCANT]    [CPU▌▌▌ RAM▌▌▌ DISK▌▌▌ | ▌82°F ▌79°F]    [⚙ 📋]
 left title         center NAS panel                    right icons
```

**NAS instrument panel design (center section):**
- Use CSS flexbox, `gap: 12px`, centered
- Each stat group: small label in amber at 60% opacity (`font-size: 9px`, uppercase) above a vertical bar
- **Vertical bar gauges** for CPU%, RAM%, Disk%:
  - Bar: 4px wide × 20px tall, background `rgba(232,160,32,0.15)`, fill amber from bottom
  - Numeric value below bar: `font-size: 10px`, amber, monospace
  - Example: `CPU` label → 20px bar → `34%` value
- **Separator** between usage stats and temp stats: `1px solid rgba(232,160,32,0.25)`, height 24px
- **Drive temp bars** (one per drive in mock data):
  - Same 4px × 20px bar, fill mapped: 32°F=0% → 140°F=100% (reasonable NAS range)
  - Label: volume name from mock data (should be "TheRock" — update mock)
  - Temp value below: `{n}°F`, `font-size: 10px`, amber
  - Color: amber below 95°F, orange `#FF8C00` at 95–113°F, red above 113°F
- Total width of NAS panel: ~180–200px max, must not overflow 800px header

**When showBack is true:** hide the NAS panel entirely (detail/settings/logs pages — irrelevant there)

**Update mock generator:**
- Change volume label from `vol1` (or whatever it currently is) to `TheRock`
- Convert any Celsius temp mock values to Fahrenheit (multiply × 9/5 + 32, or just set realistic °F values directly: 86–104°F is typical NAS drive temp range)
- Ensure the SSE payload includes `nasStats` with: `cpu: number`, `ram: number`, `disk: number`, `drives: Array<{label: string, tempF: number, usedPct: number}>`

**Type update:** If `packages/shared/src/types.ts` NasStats type uses tempC, add tempF or change to tempF.
  </action>
  <verify>
    - AppHeader renders NAS panel in middle with vertical bars visible
    - Volume shows "TheRock" not "vol1"
    - Temperature shown as °F not °C
    - showBack=true hides the NAS panel
  </verify>
  <done>AppHeader middle section shows compact NAS instrument panel with vertical bar gauges, TheRock label, and °F temperatures.</done>
</task>

<task type="auto">
  <name>Task 2: Remove NAS card from CardGrid</name>
  <files>
    packages/frontend/src/components/cards/CardGrid.tsx
    packages/frontend/src/components/cards/ServiceCard.tsx
  </files>
  <read_first>
    - packages/frontend/src/components/cards/CardGrid.tsx
    - packages/frontend/src/components/cards/ServiceCard.tsx
  </read_first>
  <action>
Remove the NAS card from the dashboard card grid since its data now lives in the header.

In `CardGrid.tsx`:
- Filter out the NAS service entry from the rendered cards (match by `service.id === 'nas'` or equivalent)
- If NAS is in a dedicated section header, remove that section too if it becomes empty
- The remaining cards (Plex, Radarr, Sonarr, Lidarr, Bazarr, SABnzbd, Pi-hole) continue as before

In `ServiceCard.tsx`:
- Keep the NAS instrument body renderer (`renderInstrumentBody` case for 'nas') intact — it may be used on the detail page. Just ensure it isn't rendered in the card grid.
  </action>
  <verify>
    - Dashboard grid no longer shows NAS card
    - All other cards still render
    - No console errors about missing NAS data
  </verify>
  <done>NAS card removed from CardGrid. Header is the sole display of NAS stats on the dashboard.</done>
</task>

</tasks>

<verification>
NAS stats visible in header as compact instrument panel. No NAS card in grid. "TheRock" label used. All temps in °F.
</verification>

<success_criteria>
- Header middle shows vertical bar gauges for CPU/RAM/Disk + drive temp bars
- Volume/NAS label is "TheRock"
- Temperatures displayed in °F
- NAS card absent from dashboard grid
- Header stays single-row, no overflow at 800px
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-11-SUMMARY.md`
</output>
