---
phase: 02-core-ui-shell
plan: 09
type: execute
wave: 3
depends_on:
  - "02-06"
  - "02-07"
files_modified:
  - packages/frontend/src/components/layout/NowPlayingBanner.tsx
  - packages/frontend/src/components/layout/StreamRow.tsx
  - packages/frontend/src/pages/ServiceDetailPage.tsx
  - packages/frontend/src/components/ui/StaleIndicator.tsx
autonomous: true
gap_closure: true
requirements:
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Now Playing banner uses cockpit amber palette — no cyan/Tron blue"
    - "Stream rows use utilitarian format: USER > TITLE S0XEX left, QUAL / DIRECT right, 1px amber progress bar"
    - "Service detail page uses dot-leader readout format: LABEL ........... VALUE"
    - "Stale indicator uses cockpit amber badge styling"
  artifacts:
    - path: "packages/frontend/src/components/layout/NowPlayingBanner.tsx"
      provides: "Cockpit-styled Now Playing banner"
      contains: "cockpit-amber"
    - path: "packages/frontend/src/components/layout/StreamRow.tsx"
      provides: "Utilitarian stream row with amber progress"
      contains: "cockpit-amber"
    - path: "packages/frontend/src/pages/ServiceDetailPage.tsx"
      provides: "Dot-leader readout detail page"
      contains: "dot-leader"
    - path: "packages/frontend/src/components/ui/StaleIndicator.tsx"
      provides: "Cockpit-styled stale badge"
      contains: "cockpit-amber"
  key_links:
    - from: "packages/frontend/src/pages/ServiceDetailPage.tsx"
      to: "packages/frontend/src/components/ui/StatusDot.tsx"
      via: "import StatusDot (already cockpit-styled from Plan 07)"
      pattern: "StatusDot"
---

<objective>
Restyle the Now Playing banner, stream rows, service detail page, and stale indicator to match the Star Wars cockpit aesthetic.

Purpose: Complete the visual retheme of all remaining UI surfaces. After this plan, every visible component uses the cockpit palette.
Output: Cockpit-styled banner, utilitarian stream rows, dot-leader detail readouts, amber stale badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-ui-shell/02-CONTEXT.md

@packages/frontend/src/components/layout/NowPlayingBanner.tsx
@packages/frontend/src/components/layout/StreamRow.tsx
@packages/frontend/src/pages/ServiceDetailPage.tsx
@packages/frontend/src/components/ui/StaleIndicator.tsx
@packages/shared/src/types.ts

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface PlexStream {
  user: string
  title: string
  year?: number
  season?: number
  episode?: number
  progressPercent: number
  quality: string
  transcode: boolean
}

export interface ServiceStatus {
  id: string
  name: string
  tier: 'status' | 'activity' | 'rich'
  status: 'online' | 'offline' | 'warning' | 'stale'
  lastPollAt: string
  metrics?: Record<string, unknown>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle NowPlayingBanner and StreamRow for cockpit aesthetic</name>
  <files>packages/frontend/src/components/layout/NowPlayingBanner.tsx, packages/frontend/src/components/layout/StreamRow.tsx</files>
  <read_first>
    - packages/frontend/src/components/layout/NowPlayingBanner.tsx
    - packages/frontend/src/components/layout/StreamRow.tsx
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
**NowPlayingBanner.tsx restyle (per D-31 through D-33):**

Update all colors/styling. Keep the existing structure (AnimatePresence, collapsed/expanded, backdrop, ticker).

Color changes:
- Banner background: `rgba(13, 13, 13, 0.95)` (was `rgba(13, 17, 23, 0.95)`)
- Banner border-top: `1px solid rgba(232, 160, 32, 0.30)` (was cyan 0.15)
- Backdrop overlay: keep `rgba(0, 0, 0, 0.4)` (fine as-is)

Collapsed strip (per D-32):
- Stream count label: `color: var(--cockpit-amber)` (was `var(--tron-blue)`)
- Ticker text: `color: var(--text-offwhite)` (was `var(--text-muted)`)

Expanded drawer (per D-33):
- Add amber top border on drawer: `borderTop: '1px solid var(--cockpit-amber)'`
- Keep maxHeight constraint `min(60vh, 320px)`

Replace ALL `var(--tron-blue)` with `var(--cockpit-amber)`.
Replace ALL `var(--tron-amber)` with `var(--cockpit-amber)` (same target in this palette).
Replace ALL `var(--text-muted)` with `var(--text-offwhite)`.
Replace `rgba(0, 200, 255, ...)` with amber equivalents.

**StreamRow.tsx restyle (per D-34):**

Update to utilitarian cockpit format.

Row layout change (per D-34): `USER > TITLE S0XEX` on left, `QUAL / DIRECT` on right.
- First line: `{stream.user} > {titleText}` left-aligned, quality + transcode right-aligned
- Format: `<span style={{ color: 'var(--cockpit-amber)' }}>{stream.user}</span> <span style={{ color: 'var(--text-offwhite)' }}>&gt; {titleText}</span>` on the left
- Right side: `<span>{stream.quality}</span> / <span>{stream.transcode ? 'TRANSCODE' : 'DIRECT'}</span>`
- Transcode label: `color: var(--cockpit-amber)` for TRANSCODE, `color: var(--cockpit-green)` for DIRECT

Progress bar change (per D-34):
- Height: `1px` (was 4px) — subtle amber line
- Bar background: `rgba(232, 160, 32, 0.15)` (was cyan)
- Bar fill: `var(--cockpit-amber)` (was `var(--tron-blue)`)

Row divider:
- `borderBottom: '1px solid rgba(232, 160, 32, 0.08)'` (was cyan 0.08)

Remove ALL `var(--tron-blue)`, `var(--tron-amber)`, `var(--text-muted)` references.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tron" packages/frontend/src/components/layout/NowPlayingBanner.tsx; grep -c "tron" packages/frontend/src/components/layout/StreamRow.tsx; grep -q "cockpit-amber" packages/frontend/src/components/layout/NowPlayingBanner.tsx && echo "PASS: banner amber" || echo "FAIL"; grep -q "cockpit-amber" packages/frontend/src/components/layout/StreamRow.tsx && echo "PASS: stream amber" || echo "FAIL"; grep -q "1px" packages/frontend/src/components/layout/StreamRow.tsx && echo "PASS: 1px progress" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "tron" in NowPlayingBanner.tsx returns 0 matches
    - grep for "tron" in StreamRow.tsx returns 0 matches
    - grep for "rgba(0, 200, 255" in NowPlayingBanner.tsx returns 0 matches
    - grep for "rgba(0, 200, 255" in StreamRow.tsx returns 0 matches
    - NowPlayingBanner uses "cockpit-amber" for stream count label
    - NowPlayingBanner uses "text-offwhite" for ticker text
    - StreamRow contains ">" character in user/title format
    - StreamRow progress bar height is "1px"
    - StreamRow uses "cockpit-amber" for progress fill
    - StreamRow uses "cockpit-green" for DIRECT label color
  </acceptance_criteria>
  <done>NowPlayingBanner and StreamRow fully restyled with cockpit palette. Banner uses amber chrome, stream rows use utilitarian USER > TITLE format with 1px amber progress bars.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ServiceDetailPage with dot-leader readouts and update StaleIndicator</name>
  <files>packages/frontend/src/pages/ServiceDetailPage.tsx, packages/frontend/src/components/ui/StaleIndicator.tsx</files>
  <read_first>
    - packages/frontend/src/pages/ServiceDetailPage.tsx
    - packages/frontend/src/components/ui/StaleIndicator.tsx
    - .planning/phases/02-core-ui-shell/02-CONTEXT.md
  </read_first>
  <action>
**ServiceDetailPage.tsx restyle (per D-35 through D-37):**

Update to cockpit palette with dot-leader readout format.

Back button removal:
- Remove the standalone `<button>` back link at the top (back nav is now handled by AppHeader showBack prop from Plan 07)

Page title:
- Color: `var(--cockpit-amber)` (was implicit tron-blue from html color)
- Keep `text-heading` class, `text-transform: uppercase` (per D-29)

Readout rows (per D-36) — replace the current simple label:value pairs with dot-leader format:
```
LABEL ........... VALUE
```

Implementation for dot-leader:
- Each readout row: `display: flex; align-items: baseline`
- Label: `<span className="text-label" style={{ color: 'var(--text-offwhite)', textTransform: 'uppercase', flexShrink: 0 }}>`
- Dot leader: `<span style={{ flex: 1, borderBottom: '1px dotted rgba(232,160,32,0.20)', margin: '0 8px', minWidth: '20px' }} />`
- Value: `<span className="text-body" style={{ color: 'var(--cockpit-amber)', flexShrink: 0 }}>`

Apply to all mock metric slots:
- STATUS ........... {status}
- LAST CHECKED ........... {time}
- RESPONSE TIME ........... ---
- UPTIME ........... ---
- VERSION ........... ---

Placeholder text at bottom: color `var(--text-offwhite)` (was `var(--text-muted)`)

Remove ALL `var(--tron-blue)`, `var(--text-muted)` references.

**StaleIndicator.tsx restyle:**
- Badge color: `var(--cockpit-amber)` (was `var(--tron-amber)` / `#ffaa00`)
- Badge text color: `#0D0D0D` (dark text on amber badge for contrast) or amber text on transparent — match the cockpit aesthetic
- Style: `color: var(--cockpit-amber)`, `border: 1px solid var(--cockpit-amber)`, `padding: 1px 6px`, `border-radius: 3px`, `text-transform: uppercase`
- Keep the `text-label` class, keep the 5-minute stale threshold logic
- Remove any `var(--tron-amber)` references
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -c "tron" packages/frontend/src/pages/ServiceDetailPage.tsx; grep -c "tron" packages/frontend/src/components/ui/StaleIndicator.tsx; grep -q "dotted" packages/frontend/src/pages/ServiceDetailPage.tsx && echo "PASS: dot-leader" || echo "FAIL"; grep -q "cockpit-amber" packages/frontend/src/pages/ServiceDetailPage.tsx && echo "PASS: amber" || echo "FAIL"; grep -q "cockpit-amber" packages/frontend/src/components/ui/StaleIndicator.tsx && echo "PASS: stale amber" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "tron" in ServiceDetailPage.tsx returns 0 matches
    - grep for "tron" in StaleIndicator.tsx returns 0 matches
    - ServiceDetailPage contains "dotted" (dot-leader border style)
    - ServiceDetailPage contains "cockpit-amber" for values
    - ServiceDetailPage contains "text-offwhite" for labels
    - ServiceDetailPage does NOT contain a standalone back button element (back nav is in AppHeader)
    - StaleIndicator uses "cockpit-amber" for styling
    - StaleIndicator contains "uppercase" text-transform
  </acceptance_criteria>
  <done>ServiceDetailPage uses dot-leader readout format with cockpit palette. StaleIndicator badge restyled with amber cockpit colors. No standalone back button (handled by AppHeader).</done>
</task>

</tasks>

<verification>
- `grep -rc "tron" packages/frontend/src/components/layout/NowPlayingBanner.tsx packages/frontend/src/components/layout/StreamRow.tsx packages/frontend/src/pages/ServiceDetailPage.tsx packages/frontend/src/components/ui/StaleIndicator.tsx` returns all zeros
- `grep "dotted" packages/frontend/src/pages/ServiceDetailPage.tsx` returns match
- `cd packages/frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
- NowPlayingBanner uses amber cockpit palette throughout
- StreamRow uses USER > TITLE format with 1px amber progress bar
- ServiceDetailPage uses dot-leader LABEL...VALUE readout format
- StaleIndicator uses amber cockpit styling
- No standalone back button in ServiceDetailPage (AppHeader handles it)
- Zero Tron references in any modified file
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui-shell/02-09-SUMMARY.md`
</output>
