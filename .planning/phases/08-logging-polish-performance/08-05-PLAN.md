---
phase: 08-logging-polish-performance
plan: 05
type: execute
wave: 3
depends_on: ["08-03", "08-04"]
files_modified: []
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "User confirms all Phase 8 deliverables work correctly on kiosk display"
    - "Dashboard is readable from 10+ feet"
    - "Log viewer live-tails and filters correctly"
    - "No polling artifacts visible at kiosk distance"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 8 deliverables.

Purpose: Confirm that performance tuning, visual polish, and log viewer all work correctly together on the kiosk display and mobile viewport.

Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-logging-polish-performance/08-CONTEXT.md
@.planning/phases/08-logging-polish-performance/08-01-SUMMARY.md
@.planning/phases/08-logging-polish-performance/08-02-SUMMARY.md
@.planning/phases/08-logging-polish-performance/08-03-SUMMARY.md
@.planning/phases/08-logging-polish-performance/08-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full Phase 8 verification on kiosk and mobile</name>
  <what-built>
    Complete Phase 8 deliverables:
    - Performance: NAS 1s poll, UniFi 3s poll, SSE change detection (no stale flicker), Tautulli webhook triggers instant Plex banner updates
    - Layout: Download bar removed, tiles fill freed space, NAS is standalone tile with amber header
    - Visual: Deep navy background, panel depth shadow, text glow on numerics, text scaled up for 10ft, CRT sweep bar
    - Color: NAS bars green/amber/red thresholds, UniFi multi-arrows blue/red, Plex stats colored, UniFi client green bar gauge
    - Logging: Full log viewer at /logs with live tail, level+service filters, export, purge with confirmation
    - Settings: LOGS tab with retention days configuration
    - Persistence: UniFi peak values survive server restart (kv_store), nightly 3am log prune
  </what-built>
  <how-to-verify>
    **Performance checks:**
    1. Open dashboard — NAS CPU/RAM bars should animate smoothly as live gauges (1s updates)
    2. Start a Plex stream — banner should appear within 1-3 seconds
    3. Stop the stream — banner should disappear within 1-3 seconds
    4. Watch dashboard for 30 seconds — no visible flicker or stale-data flash
    5. Open browser DevTools Network tab — SSE messages should NOT arrive every 1 second when dashboard is idle

    **Visual checks at kiosk distance (10+ ft):**
    6. Can you read NAS CPU% and RAM% clearly from 10 feet?
    7. Can you identify which tile is which from 10 feet?
    8. Is the CRT sweep bar visible as a subtle ambient effect (not distracting)?
    9. Does the deep navy background reduce glare compared to the old pure black?
    10. Do NAS bars change color visibly at different load levels?

    **Layout checks on 800x480:**
    11. No vertical scroll on the dashboard page
    12. Download bar is gone — SABnzbd shows inline progress in its tile during active download
    13. NAS is its own tile (not in the header)
    14. All tiles fill the available viewport height proportionally

    **Log viewer (/logs):**
    15. Navigate to /logs — see structured log entries
    16. Default filter is WARN+ (no info noise)
    17. Change level filter to ALL — info entries appear
    18. Change service filter — entries filter correctly
    19. Click EXPORT LOGS — JSON file downloads
    20. Click PURGE LOGS — confirmation modal appears with correct copy
    21. Wait 10 seconds — new log entries should appear at the top without page refresh

    **Settings:**
    22. Navigate to /settings — LOGS tab exists alongside SERVICES and NOTIFICATIONS
    23. LOGS tab shows retention days input with value 7
    24. Change to 14, click SAVE RETENTION — amber checkmark appears briefly

    **UniFi checks:**
    25. UniFi tile shows green bar gauge for client count (no raw number)
    26. UniFi throughput shows blue/red arrows scaling with speed
    27. Restart the server — UniFi peak values persist (bars do not reset to full)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
User approval on all visual, performance, and functional checks.
</verification>

<success_criteria>
All Phase 8 success criteria from ROADMAP.md are met:
1. Log viewer shows structured entries filterable by service and level
2. Purge works and reflects immediately
3. SQLite bounded (WAL mode, nightly prune)
4. Plex stream state <= 3s lag
5. SABnzbd progress updates smoothly
6. No polling artifacts at kiosk distance
</success_criteria>

<output>
After completion, create `.planning/phases/08-logging-polish-performance/08-05-SUMMARY.md`
</output>
