---
phase: 08-logging-polish-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - theme-preview.html
autonomous: false
requirements:
  - PERF-02

must_haves:
  truths:
    - "User can view three side-by-side theme variants in a static HTML page"
    - "Each variant renders a NAS tile, media stack card, metric tile, and AppHeader strip mockup"
    - "User selects a variant to gate all subsequent CSS background changes"
  artifacts:
    - path: "theme-preview.html"
      provides: "Static theme comparison page with three background variants"
      contains: "Variant A"
  key_links: []
---

<objective>
Static theme preview page for user to select background variant before any CSS changes.

Purpose: D-34/D-35/D-36 require a preview-first approach — the user must see three background variants side by side and select one before any production CSS is updated. This gates Plan 04 (frontend visual polish).

Output: `theme-preview.html` at project root — a standalone HTML file with no React/build dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-logging-polish-performance/08-CONTEXT.md
@.planning/phases/08-logging-polish-performance/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme-preview.html with three background variants</name>
  <files>theme-preview.html</files>
  <read_first>
    packages/frontend/src/styles/globals.css,
    packages/frontend/src/components/layout/AppHeader.tsx,
    packages/frontend/src/components/cards/ServiceCard.tsx
  </read_first>
  <action>
Create `theme-preview.html` at the project root. This is a standalone HTML file (no React, no build step). It renders three side-by-side columns, each showing the same set of dashboard component mockups against a different background:

**Variant A — Deep Navy / Amber:** background `#001133`
**Variant B — Warm Dark / Amber:** background `#0D0906`
**Variant C — Tactical Dark / Amber:** background `#000D1A`

Each column contains static HTML/CSS mockups of:
1. **AppHeader strip** — a 48px high bar with "CORUSCANT" title in amber (`#E8A020`), a simulated NAS gauge (CPU 45%, RAM 62% as colored bars), and temp readout (88F). Use JetBrains Mono font (Google Fonts CDN import for preview convenience).
2. **NAS tile** — a chamfered card with amber ribbon header, CPU/RAM bars with threshold colors (green <60%, amber 60-85%, red >85%), disk temps. Include `box-shadow: inset 0 0 20px rgba(0,0,0,0.5)` per D-16.
3. **Media stack card** — a chamfered card with amber "MEDIA" header, 6 LED rows (RADARR, SONARR, LIDARR, PROWLARR, BAZARR, READARR) with green/red dots.
4. **Metric tile** — a simulated Pi-hole/NETWORK card with a numeric readout using `text-shadow: 0 0 6px currentColor` per D-17.

**CRT sweep overlay:** Each column includes a 4px semi-transparent bar animating top-to-bottom over 10s per D-37. Variant C gets a slightly stronger sweep opacity (`rgba(255,255,255,0.04)`) vs A and B (`rgba(255,255,255,0.025)`).

**Layout:** Three equal-width columns with variant label at top (e.g., "A — DEEP NAVY", "B — WARM DARK", "C — TACTICAL DARK"). Full viewport height. Centered on page.

**Panel surface color:** All tiles use `--space-mid: #0E1520` for consistency across variants (only the body background changes).

**Styling:** Inline `<style>` block in the HTML file. Use CSS custom properties per column via scoped classes (`.variant-a`, `.variant-b`, `.variant-c`). Amber accent: `#E8A020`. Text: `#C8C8C8`. Border: `rgba(232,160,32,0.20)`.

**Text sizes:** Use the Phase 8 scaled sizes — Display 24px semibold, Heading 18px semibold, Body 15px, Label 12px — so the preview reflects final kiosk appearance.

The file should be viewable by opening directly in a browser or via `npx serve .` from the project root.
  </action>
  <verify>
    <automated>test -f /Users/Oreo/Projects/Coruscant/theme-preview.html && grep -c 'Variant' /Users/Oreo/Projects/Coruscant/theme-preview.html</automated>
  </verify>
  <acceptance_criteria>
    - theme-preview.html exists at project root
    - theme-preview.html contains `#001133` (Variant A background)
    - theme-preview.html contains `#0D0906` (Variant B background)
    - theme-preview.html contains `#000D1A` (Variant C background)
    - theme-preview.html contains `#E8A020` (amber accent)
    - theme-preview.html contains `inset 0 0 20px rgba(0,0,0,0.5)` (panel depth D-16)
    - theme-preview.html contains `text-shadow: 0 0 6px currentColor` (glow D-17)
    - theme-preview.html contains `crtSweep` (CRT sweep animation D-37)
    - theme-preview.html contains `JetBrains Mono` (font)
    - theme-preview.html contains `CORUSCANT` (AppHeader mockup)
    - theme-preview.html contains `MEDIA` (media stack mockup)
    - theme-preview.html contains no `import` or `require` statements (standalone)
  </acceptance_criteria>
  <done>
    Static HTML file renders three theme variants side by side with representative dashboard component mockups. User can view in any browser to compare backgrounds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User selects theme variant</name>
  <what-built>Three side-by-side theme variant previews showing the dashboard components against Deep Navy (#001133), Warm Dark (#0D0906), and Tactical Dark (#000D1A) backgrounds.</what-built>
  <how-to-verify>
    1. Open `theme-preview.html` in a browser: `open theme-preview.html` or navigate to it
    2. Compare the three columns at normal viewing distance (and ideally on the kiosk display if available)
    3. Note which variant has the best readability and cockpit feel at distance
    4. Look at the CRT sweep bar moving down each column — confirm it is subtle and ambient
    5. Check that the amber accent (#E8A020) works well against each background
  </how-to-verify>
  <resume-signal>Type the variant letter: "A", "B", or "C" — this gates all subsequent CSS background changes in Plan 04</resume-signal>
</task>

</tasks>

<verification>
- `theme-preview.html` opens in a browser and renders three distinct columns
- User has selected a variant (A, B, or C)
</verification>

<success_criteria>
Theme preview page exists and user has selected a background variant. The selected variant's hex value will be used as `--space-deep` in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/08-logging-polish-performance/08-02-SUMMARY.md`
Record the selected variant in the summary (e.g., "User selected Variant B: #0D0906").
</output>
