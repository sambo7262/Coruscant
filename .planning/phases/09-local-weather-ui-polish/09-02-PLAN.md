---
phase: 09-local-weather-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/frontend/src/components/cards/CardGrid.tsx
  - packages/frontend/src/styles/globals.css
  - packages/backend/src/routes/arr-webhooks.ts
  - packages/frontend/src/components/layout/NowPlayingBanner.tsx
autonomous: true
requirements:
  - WTHR-02

must_haves:
  truths:
    - "Disconnect dot is red (#ff4444), 10px, with red glow — visible at kiosk distance"
    - "AppHeader text (CORUSCANT title, nav icons) is slightly larger than dashboard body text"
    - "DOWNLOADS tile height is capped to match NETWORK tile height"
    - "Long download titles scroll via CSS marquee instead of truncating with ellipsis"
    - "Download speed number is colored Tron blue (#00c8ff)"
    - "Webhook events are logged with [WEBHOOK] SERVICE -> event_type -> title format"
    - "Text across the dashboard is sharper — no blur bleed from backdropFilter on parent containers"
  artifacts:
    - path: "packages/frontend/src/components/layout/AppHeader.tsx"
      provides: "Red disconnect dot, larger header text, backdrop blur isolation"
      contains: "#ff4444"
    - path: "packages/frontend/src/components/cards/CardGrid.tsx"
      provides: "DOWNLOADS maxHeight cap, marquee scroll, speed coloring"
      contains: "downloadsMarquee"
    - path: "packages/frontend/src/styles/globals.css"
      provides: "CSS @keyframes downloadsMarquee"
      contains: "@keyframes downloadsMarquee"
    - path: "packages/backend/src/routes/arr-webhooks.ts"
      provides: "Structured webhook logging with [WEBHOOK] prefix"
      contains: "[WEBHOOK]"
  key_links:
    - from: "packages/frontend/src/components/cards/CardGrid.tsx"
      to: "packages/frontend/src/styles/globals.css"
      via: "downloadsMarquee keyframes animation"
      pattern: "downloadsMarquee"
---

<objective>
UI polish pass: disconnect dot, header sizing, DOWNLOADS tile fixes, speed coloring, webhook log format, and text sharpness.

Purpose: Resolves ISSUE-01 through ISSUE-08, ISSUE-11, and ISSUE-14 from the mandatory micro-issue audit (D-08, D-09, D-10, D-11, D-12, D-13, D-20). These are all visual and formatting fixes that can execute in parallel with the weather backend (Plan 01) and settings restructure (Plan 03).

Output: Sharper, more legible dashboard with red disconnect dot, larger header text, height-capped DOWNLOADS with marquee scroll, colored speed numbers, and structured webhook log entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-local-weather-ui-polish/09-CONTEXT.md
@.planning/phases/09-local-weather-ui-polish/09-RESEARCH.md

<interfaces>
<!-- Key CSS and component patterns the executor needs. -->

From packages/frontend/src/styles/globals.css (existing marquee pattern):
```css
/* @keyframes marquee already exists — used by NowPlayingBanner */
```

From packages/frontend/src/components/layout/AppHeader.tsx (disconnect dot, lines ~150-158):
```typescript
// Current disconnect dot styling:
backgroundColor: 'var(--cockpit-amber)'
width: '6px', height: '6px'
boxShadow: '0 0 5px 2px rgba(232, 160, 32, 0.6)'
// Current header backdrop:
backdropFilter: 'blur(4px)'  // line 63 — causes text blur bleed
```

From packages/frontend/src/components/cards/CardGrid.tsx (DOWNLOADS section, lines ~88, ~104):
```typescript
// Current title truncation:
overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap'
// Current speed color:
color: 'var(--cockpit-amber)'  // line 104 — needs to be #00c8ff for download
```

From packages/backend/src/routes/arr-webhooks.ts:
```typescript
// Currently: no structured log format for webhook events
// Needs: fastify.log.info('[WEBHOOK] SERVICE -> event_type -> "title"')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disconnect dot + header sizing + text sharpness (D-08, D-09, D-20)</name>
  <files>
    packages/frontend/src/components/layout/AppHeader.tsx,
    packages/frontend/src/components/layout/NowPlayingBanner.tsx,
    packages/frontend/src/styles/globals.css
  </files>
  <read_first>
    packages/frontend/src/components/layout/AppHeader.tsx,
    packages/frontend/src/components/layout/NowPlayingBanner.tsx,
    packages/frontend/src/styles/globals.css
  </read_first>
  <action>
    **A. Disconnect dot — D-08 (ISSUE-01):**

    In `AppHeader.tsx`, find the disconnect dot element (around line 150-158). Change:
    - `backgroundColor` from `'var(--cockpit-amber)'` to `'#ff4444'`
    - `width` and `height` from `'6px'` to `'10px'`
    - `boxShadow` from `'0 0 5px 2px rgba(232, 160, 32, 0.6)'` to `'0 0 8px 3px rgba(255, 68, 68, 0.7)'`
    - `borderRadius` stays `'50%'`

    **B. Header text sizing — D-09 (ISSUE-14):**

    In `AppHeader.tsx`:
    - Find the CORUSCANT title element. Increase its `fontSize` from the current value (likely `24px` via `.text-display` class or inline) to `28px` inline style.
    - Find the Settings and Logs nav icon buttons. Increase their icon size from current to `26px` (or increase the SVG viewBox rendering size).
    - Ensure all header text elements are visibly larger than the `15px` body text used in dashboard tiles.

    **C. Text sharpness — D-20 (ISSUE-02, ISSUE-03):**

    In `AppHeader.tsx`:
    - Find the `backdropFilter: 'blur(4px)'` style (around line 63).
    - Move the blur to a `::before` pseudo-element approach: add a CSS class `.header-blur-bg` in globals.css that uses `position: absolute; inset: 0; z-index: -1; backdrop-filter: blur(4px);` and apply it to a new `<div>` sibling inside the header, positioned behind the content. Alternatively, add `isolation: 'isolate'` to the text container elements within the header so the blur does not bleed into text rendering.
    - Preferred approach: Remove `backdropFilter` from the header's inline style entirely. Instead, create a `::before` pseudo on the header's CSS class:
      ```css
      .app-header-blur::before {
        content: '';
        position: absolute;
        inset: 0;
        backdrop-filter: blur(4px);
        -webkit-backdrop-filter: blur(4px);
        z-index: -1;
      }
      ```
      Add `className="app-header-blur"` to the header container and `position: 'relative'` to its style. Remove the inline `backdropFilter`.

    In `NowPlayingBanner.tsx`:
    - Apply the same `::before` pseudo-element pattern to isolate `backdropFilter: 'blur(8px)'` (line ~136). Create `.banner-blur-bg::before` with `backdrop-filter: blur(8px)`.
    - Remove the inline `backdropFilter` from the banner's style.

    In `globals.css`:
    - Keep `-webkit-font-smoothing: antialiased` (ISSUE-04 analysis: antialiased is correct for Retina-class kiosk display).
    - Add the `.app-header-blur::before` and `.banner-blur-bg::before` classes described above.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -n "ff4444\|10px.*disconnect\|app-header-blur\|banner-blur-bg" packages/frontend/src/components/layout/AppHeader.tsx packages/frontend/src/components/layout/NowPlayingBanner.tsx packages/frontend/src/styles/globals.css | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.tsx contains `#ff4444` for disconnect dot background color
    - AppHeader.tsx disconnect dot has width/height of `10px`
    - AppHeader.tsx disconnect dot has `box-shadow` with `rgba(255, 68, 68, 0.7)`
    - AppHeader.tsx does NOT have inline `backdropFilter: 'blur(4px)'` on the header container (moved to CSS pseudo)
    - globals.css contains `.app-header-blur::before` with `backdrop-filter: blur(4px)`
    - NowPlayingBanner.tsx does NOT have inline `backdropFilter: 'blur(8px)'` (moved to CSS pseudo)
    - globals.css contains `.banner-blur-bg::before` with `backdrop-filter: blur(8px)`
    - CORUSCANT title fontSize is 28px or larger
    - `npm run test --workspace=packages/frontend -- --run` exits 0
  </acceptance_criteria>
  <done>Disconnect dot is red and 10px with red glow, header text is larger, backdrop blur is isolated to pseudo-elements so text renders sharply</done>
</task>

<task type="auto">
  <name>Task 2: DOWNLOADS tile cap + marquee + speed colors + webhook log format (D-10, D-11, D-12, D-13)</name>
  <files>
    packages/frontend/src/components/cards/CardGrid.tsx,
    packages/frontend/src/styles/globals.css,
    packages/backend/src/routes/arr-webhooks.ts
  </files>
  <read_first>
    packages/frontend/src/components/cards/CardGrid.tsx,
    packages/frontend/src/styles/globals.css,
    packages/backend/src/routes/arr-webhooks.ts,
    packages/backend/src/poll-manager.ts
  </read_first>
  <action>
    **A. DOWNLOADS tile max-height — D-10 (ISSUE-05):**

    In `CardGrid.tsx`, find the DOWNLOADS / Media tile container div (around lines 174-207). Add `maxHeight` style that constrains the tile to match the NETWORK tile height. Use a CSS custom property approach:
    - In the CardGrid component, add a CSS custom property `--tile-max-height` set to a reasonable value. The NETWORK tile is roughly 280-320px tall depending on content. Use `maxHeight: 'var(--tile-max-height, 320px)'` on the DOWNLOADS tile container.
    - Add `overflow: 'hidden'` to the container so excess content is clipped.
    - Alternatively, if both tiles are in a CSS grid row, use `align-items: start` on the grid and let the NETWORK tile set the natural height, with DOWNLOADS capped via `maxHeight: '100%'` within the grid cell if they share a row.

    **B. DOWNLOADS title marquee — D-11 (ISSUE-06):**

    In `CardGrid.tsx`, find the download title element (around line 88) that currently has `textOverflow: 'ellipsis'`. Replace with a marquee scroll approach:
    - Remove `overflow: 'hidden'`, `textOverflow: 'ellipsis'`, `whiteSpace: 'nowrap'` from the title element.
    - Wrap the title text in a container div with `overflow: 'hidden'`.
    - Inside, render a `<span>` with `display: 'inline-block'`, `whiteSpace: 'nowrap'`.
    - Detect if title is long (> ~25 characters is a reasonable heuristic, or use a ref to measure overflow).
    - When title is long, apply `animation: 'downloadsMarquee 8s linear infinite'`, `animationDelay: '2s'`.
    - When title is short, no animation — just static text.

    In `globals.css`, add the marquee keyframes:
    ```css
    @keyframes downloadsMarquee {
      0%   { transform: translateX(0); }
      10%  { transform: translateX(0); }
      90%  { transform: translateX(calc(-100% + 100px)); }
      100% { transform: translateX(calc(-100% + 100px)); }
    }
    ```
    The `calc(-100% + 100px)` ensures the text scrolls enough to reveal the end while keeping part visible.

    **C. Speed number coloring — D-12 (ISSUE-07, ISSUE-08):**

    In `CardGrid.tsx`, find the speed number display (around line 104). Change:
    - Download speed `color` from `'var(--cockpit-amber)'` to `'#00c8ff'` (Tron blue per D-12).
    - If upload speed is displayed, color it `'#E8A020'` (amber). Per RESEARCH open question 1, SABnzbd likely doesn't expose upload speed — in that case, just recolor the download speed to blue.

    **D. Webhook log format — D-13 (ISSUE-11):**

    In `packages/backend/src/routes/arr-webhooks.ts`, find the webhook POST handler (around line 36). After parsing the webhook body, add a structured log line using the Fastify logger:
    ```typescript
    const eventType = classifyArrEvent(body.eventType || '')
    const title = extractArrTitle(body) || 'unknown'
    fastify.log.info(
      { service: service.toUpperCase() },
      `[WEBHOOK] ${service.toUpperCase()} → ${eventType} → "${title}"`
    )
    ```
    Import `classifyArrEvent` and `extractArrTitle` from `../poll-manager.js` (they are already exported). If they are not imported already, add the import.
  </action>
  <verify>
    <automated>cd /Users/Oreo/Projects/Coruscant && grep -n "downloadsMarquee\|#00c8ff\|WEBHOOK" packages/frontend/src/components/cards/CardGrid.tsx packages/frontend/src/styles/globals.css packages/backend/src/routes/arr-webhooks.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - CardGrid.tsx DOWNLOADS container has `maxHeight` style applied (not unbounded)
    - CardGrid.tsx download title element uses `downloadsMarquee` animation instead of `textOverflow: 'ellipsis'`
    - globals.css contains `@keyframes downloadsMarquee`
    - CardGrid.tsx download speed color is `#00c8ff` (not `var(--cockpit-amber)`)
    - arr-webhooks.ts contains `[WEBHOOK]` in a fastify.log.info call
    - arr-webhooks.ts webhook log includes service name, event type, and title
    - `npm run test --workspace=packages/backend -- --run` exits 0
    - `npm run test --workspace=packages/frontend -- --run` exits 0
  </acceptance_criteria>
  <done>DOWNLOADS tile height is capped, long titles scroll via CSS marquee, download speed is Tron blue, webhook events are logged with structured [WEBHOOK] format</done>
</task>

</tasks>

<verification>
- `grep '#ff4444' packages/frontend/src/components/layout/AppHeader.tsx` confirms red disconnect dot
- `grep 'downloadsMarquee' packages/frontend/src/styles/globals.css` confirms keyframes exist
- `grep '#00c8ff' packages/frontend/src/components/cards/CardGrid.tsx` confirms blue speed color
- `grep 'WEBHOOK' packages/backend/src/routes/arr-webhooks.ts` confirms structured log format
- `grep 'app-header-blur' packages/frontend/src/styles/globals.css` confirms blur pseudo-element
- All existing tests pass: `npm test`
</verification>

<success_criteria>
- Disconnect dot is red, 10px, with red glow
- Header text (title, icons) is visibly larger than body text
- Backdrop blur isolated to pseudo-elements for sharp text rendering
- DOWNLOADS tile height-capped to match NETWORK tile
- Long download titles use CSS marquee scroll
- Download speed colored Tron blue (#00c8ff)
- Webhook events logged with [WEBHOOK] SERVICE -> event -> "title" format
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-weather-ui-polish/09-02-SUMMARY.md`
</output>
