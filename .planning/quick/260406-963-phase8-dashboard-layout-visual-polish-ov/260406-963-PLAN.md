---
type: quick
autonomous: true
files_modified:
  - packages/frontend/src/App.tsx
  - packages/frontend/src/components/layout/AppHeader.tsx
  - packages/frontend/src/components/layout/NowPlayingBanner.tsx
  - packages/frontend/src/components/cards/CardGrid.tsx
  - packages/frontend/src/components/cards/ServiceCard.tsx
  - packages/frontend/src/pages/DashboardPage.tsx
---

<objective>
Overhaul the dashboard layout and visual polish based on Phase 8 UAT feedback.

Purpose: Implement the Variant C cockpit aesthetic throughout, restructure the dashboard into the approved top-to-bottom layout (header → NAS full-width → 2-col media+network → Plex rail), remove the download tile, and enrich the NAS tile with vertical bars and better docker stats.

Output: A single-page dashboard with the correct layout, all visual improvements, and download activity shown inline within the Media tile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@packages/frontend/src/App.tsx
@packages/frontend/src/components/layout/AppHeader.tsx
@packages/frontend/src/components/layout/NowPlayingBanner.tsx
@packages/frontend/src/components/cards/CardGrid.tsx
@packages/frontend/src/components/cards/ServiceCard.tsx
@packages/frontend/src/pages/DashboardPage.tsx
@theme-preview.html

<interfaces>
<!-- Key types relevant to this task -->

From packages/shared/src/types.ts (inferred from usage):
- NasStatus: { cpu, ram, volumes: NasVolume[], disks: { id, name, tempC }[], fans: { rpm }[], docker?: { cpuPercent, ramPercent }, imageUpdateAvailable? }
- NasVolume: { name, usedPercent, tempF? }
- DashboardSnapshot: { services: ServiceStatus[], nas: NasStatus | null, streams: PlexStream[], plexServerStats?: PlexServerStats }
- ServiceStatus: { id, name, status, configured?, metrics? }
- ArrWebhookEvent: { service, eventCategory, title?, rawEventType? }
- PlexServerStats: { processCpuPercent, processRamPercent, bandwidthMbps }

Arr service IDs: radarr, sonarr, lidarr, bazarr, prowlarr, readarr
Download-relevant arr IDs: radarr, sonarr, lidarr (have activeDownloads/downloadProgress in metrics)
SABnzbd metrics: { speedMBs, queueCount, timeLeft, currentFilename, progressPercent }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim AppHeader — strip NAS gauges, keep ticker + nav</name>
  <files>packages/frontend/src/components/layout/AppHeader.tsx, packages/frontend/src/App.tsx</files>
  <action>
Remove the NAS temp/fan summary line entirely from AppHeader (the second `div` block inside `header` that renders below the title row at lines 315–369). The header becomes a single 44px title bar: CORUSCANT title left, connection/ticker center, settings+logs icons right.

Also remove the `nas`, `nasConfigured` props from AppHeaderProps (they are no longer needed — NAS data lives only in the NAS tile). Keep `connected`, `showBack`, `lastArrEvent` props. Remove the VerticalBar, GaugeColumn helper components and the tempColor function from AppHeader since they move to ServiceCard.tsx.

In App.tsx: remove `nasConfigured` derivation and `nas={snapshot?.nas ?? null}` and `nasConfigured={nasConfigured}` from the AppHeader JSX. Update the `paddingTop` on `<main>` from `'76px'` to `'52px'` (44px header + 8px breathing room).
  </action>
  <verify>TypeScript compiles: `cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -20`</verify>
  <done>Header is a single slim 44px bar. No NAS metrics visible in header. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: NAS tile full-width with vertical bars and better docker stats</name>
  <files>packages/frontend/src/components/cards/ServiceCard.tsx, packages/frontend/src/components/cards/CardGrid.tsx</files>
  <action>
**NasTileInstrument overhaul in ServiceCard.tsx:**

Replace the current `NasTileInstrument` function (lines 108–177) with a new layout:

1. **Top section — vertical column bars** (CPU, RAM, each volume):
   - Use a `display: flex; gap: 12px; align-items: flex-end` row of GaugeColumn components
   - Each GaugeColumn: label above (9px mono uppercase), tall vertical bar (8px wide × 60px tall track), value below (11px mono)
   - CPU bar color: `getBarColor(nasStatus.cpu)` (green/amber/red thresholds)
   - RAM bar color: `getBarColor(nasStatus.ram)`
   - Volume bars: `getBarColor(vol.usedPercent)` — label is `vol.name.slice(0,8)`
   - Bar fill from bottom using `position: absolute; bottom: 0; height: {pct}%`

2. **Middle section — disk temps** (existing style, keep):
   - Same as current: `display: flex; gap: 8px; flexWrap: wrap`
   - Each disk: label (disk.name or disk.id, 9px, rgba(200,200,200,0.4)) + value (11px amber glow)

3. **Bottom section — Docker container stats** (new):
   - Render only when `nasStatus.dockerContainers` exists (field added by quick task 260405-b24)
   - Check for `nasStatus.docker` as fallback (aggregate cpuPercent, ramPercent) — show aggregate row if individual containers not available
   - If `nasStatus.dockerContainers` is an array: show each container as a `display: flex; gap: 8px` row:
     `<LED> <name truncated 16 chars> <cpu glow amber> <mem glow amber>`
     Container LED: green if running, amber if paused/exited
   - If only `nasStatus.docker` available: show aggregate line `DCK CPU x.x% RAM x.x%` as before

Check what field name the SYNO docker adapter actually populates by reading the NasStatus shared type (`packages/shared/src/index.ts` or similar). If the field is simply `docker: { cpuPercent, ramPercent }` without a per-container array, keep the aggregate display but style it larger and with `text-glow` class.

**CardGrid.tsx overhaul — new layout:**

Replace the existing `return` block entirely with:

```
<div style={{ padding: '0 8px', display: 'flex', flexDirection: 'column', gap: '8px' }}>

  {/* Row 1: NAS — full width */}
  {nasService && (
    <NasFullWidthTile service={nasService} nasStatus={nasStatus} />
  )}

  {/* Row 2: 2-column — Media left, Network right */}
  <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '8px', alignItems: 'stretch' }}>
    {/* Media tile */}
    <MediaTile arrServices={arrServices} lastArrEvent={lastArrEvent} snapshot={snapshot} />
    {/* Network tile */}
    {piholeService && (
      <ServiceCard service={piholeService} index={...} allServices={snapshot.services} />
    )}
  </div>
</div>
```

Extract these as inline JSX (not new component files). The NAS tile in full-width should use `gridColumn: '1 / -1'` or just be a flex child with `width: 100%`.

Remove SABnzbd (`sabnzbd`) from the grid entirely — it is now absorbed into the Media tile's download activity display (next task). The `nonArrServices` filter should exclude `sabnzbd`, `pihole`-gets-its-own-slot, `unifi` (already excluded).

**MediaTile inside CardGrid:**

The Media tile is the existing chamfer-card MEDIA panel. Add a download activity section below the LED rows:

After the two-column arr LED rows, add a horizontal rule `<div style={{ borderTop: '1px solid rgba(232,160,32,0.08)', margin: '4px 0' }} />` and then a "DOWNLOADS" sub-section:

For each arr service in `[radarr, sonarr, lidarr]` that has `metrics.downloading === true && metrics.activeDownloads > 0`:
- Show a compact row: `<service name 8px uppercase amber> <progress bar 40px tall 3px> <quality+count 9px>`
- Progress bar uses `metrics.downloadProgress` for fill width, purple fill with glow

If SABnzbd is in snapshot and has activity (speedMBs > 0 or queueCount > 0):
- Show SABnzbd compact row: `SAB` label + speed `{speedMBs.toFixed(1)} MB/s` + progress bar

If nothing is downloading, show `<span style 9px #333 uppercase>IDLE</span>` in the downloads area.
  </action>
  <verify>TypeScript compiles: `cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -20`</verify>
  <done>NAS tile spans full width. CPU/RAM/volume bars are vertical columns. Docker stats visible. Media tile has download activity section. SABnzbd removed from standalone grid. Network/pihole tile is right column.</done>
</task>

<task type="auto">
  <name>Task 3: Visual polish — Variant C styling, Plex stat colors, Network tile, DNS blocker display</name>
  <files>packages/frontend/src/components/layout/NowPlayingBanner.tsx, packages/frontend/src/components/cards/ServiceCard.tsx</files>
  <action>
**NowPlayingBanner.tsx — Plex stat colors:**

In both the idle state and the collapsed strip, apply colors to the server stats:
- CPU: `color: '#4ADE80'` (green)
- RAM: `color: '#00c8ff'` (blue)
- Bandwidth/BW: `color: '#C8C8C8'` (white/offwhite)

Also in the expanded drawer stats section, apply the same colors to the three span elements.

**ServiceCard.tsx — NetworkInstrument visual polish (Variant C):**

Update `NetworkInstrument` to match theme-preview.html Variant C styling:

1. **DNS query blocker section** — elevate size and style:
   - BLOCKING status value: `fontSize: '20px', fontWeight: 600, letterSpacing: '0.08em'` with glow. Green when blocking, red when disabled.
   - QPM value displayed as `fontSize: '20px'` with `text-glow` and amber color
   - Sub-labels (PI-HOLE, QPM, BLOCKING) remain small uppercase

2. **Clients bar — green** (already implemented per D-39): confirm it uses `#4ADE80` with `boxShadow: '0 0 4px #4ADE80'`. If not, fix it.

3. **Network speeds — blue/red with glow** (already implemented with ThroughputBar using `#00c8ff` RX and `#FF3B3B` TX):
   - Increase speed value font size from `8px` to `10px` for readability
   - Add `text-shadow: 0 0 6px currentColor` to the speed value spans

4. **Arrow indicators** — already use `getArrowTier` with glow. Confirm `text-glow` class is applied.

**ServiceCard.tsx — remove DOWNLOADS banner tile:**

In the ServiceCard render (the generic card case), remove the branch that renders `DOWNLOADS` as the banner header for `sabnzbd` (since SABnzbd is no longer a standalone grid card — it's absorbed into the Media tile). If `service.id === 'sabnzbd'` reaches `ServiceCard`, return null.

Also remove the min-height special-case for `sabnzbd` in the card styles.
  </action>
  <verify>TypeScript compiles: `cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -20`</verify>
  <done>Plex rail stats have green CPU / blue RAM / white bandwidth. DNS blocking status is prominent (20px). Network speeds show with glow. No standalone SABnzbd card in the grid.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend` — zero TypeScript errors
2. `npm run test --workspace=packages/frontend -- --passWithNoTests` — passes
3. Visual layout (from code inspection): header is single 44px bar, NAS tile is full-width, 2-col row has media+network, Plex rail at bottom
4. AppHeader no longer accepts or uses `nas` or `nasConfigured` props
5. SABnzbd does not appear as a standalone card in CardGrid
</verification>

<success_criteria>
- Header: slim 44px, CORUSCANT title + settings/log icons only, no NAS gauges
- NAS tile: full page width, vertical bars for CPU/RAM/volumes, disk temps, docker stats
- Layout order: Header → NAS → [Media | Network] → Plex rail
- Media tile: LED rows for all arr services + download activity section at bottom (inline SABnzbd + active arr downloads)
- Network tile: large prominent DNS blocking % + client green bar + blue/red glowing WAN speeds
- Plex bottom rail: CPU=green, RAM=blue, BW=white text colors
- Zero TypeScript compile errors
</success_criteria>

<output>
After completion, commit with message: `fix(dashboard): layout overhaul — full-width NAS, vertical bars, inline downloads, Variant C polish`
</output>
