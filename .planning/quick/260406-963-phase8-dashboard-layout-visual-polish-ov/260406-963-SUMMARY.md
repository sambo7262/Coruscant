---
type: quick
id: 260406-963
date: 2026-04-06
status: complete
tags: [frontend, layout, ui, nas, media, network, plex, sabnzbd, variant-c]
key-decisions:
  - SABnzbd card eliminated from standalone grid — download activity shown inline in Media tile
  - NAS tile vertical bars replace horizontal bars — 8px wide x 60px tall tracks per metric
  - AppHeader slimmed to 44px single bar — NAS metrics moved entirely to the NAS tile
  - NetworkInstrument BLOCKING/QPM promoted to 20px with glow for cockpit readability at distance
  - Plex rail stats use semantic colors — CPU=green, RAM=blue, BW=offwhite
key-files:
  created: []
  modified:
    - packages/frontend/src/components/layout/AppHeader.tsx
    - packages/frontend/src/App.tsx
    - packages/frontend/src/components/cards/CardGrid.tsx
    - packages/frontend/src/components/cards/ServiceCard.tsx
    - packages/frontend/src/components/layout/NowPlayingBanner.tsx
metrics:
  duration: ~12min
  completed: 2026-04-06
  tasks: 3
  files: 5
---

# Quick Task 260406-963: Dashboard Layout and Visual Polish Overhaul Summary

**One-liner:** Full-width NAS tile with vertical gauge bars, 2-col Media+Network layout, inline SABnzbd downloads, Plex stat colors, and prominent DNS blocking status — Variant C cockpit aesthetic throughout.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Slim AppHeader — strip NAS gauges, keep ticker + nav | 5b595ec | AppHeader.tsx, App.tsx |
| 2 | NAS tile full-width with vertical bars + CardGrid restructure | 7bfab1f | CardGrid.tsx, ServiceCard.tsx |
| 3 | Visual polish — Variant C styling, Plex stat colors, DNS tile | 300dc29 | NowPlayingBanner.tsx, ServiceCard.tsx |

## What Was Built

### AppHeader (Task 1)
- Stripped the NAS temp/fan summary line (the second div block that rendered below the title row)
- Removed `nas`, `nasConfigured` props from `AppHeaderProps` — NAS data now lives only in the NAS tile
- Removed `VerticalBar`, `GaugeColumn`, `tempColor` helper functions from AppHeader (moved to ServiceCard)
- Header is now a single slim 44px bar: CORUSCANT title left, connection dot center, settings+logs icons right
- Updated `App.tsx` paddingTop on `<main>` from `76px` to `52px`

### CardGrid restructure (Task 2)
- Rewritten as `display: flex; flex-direction: column` layout (not grid)
- Row 1: NAS tile — full page width via `ServiceCard` with `service.id === 'nas'`
- Row 2: 2-column grid — Media tile left, Pi-hole/Network tile right
- SABnzbd removed from standalone grid entirely — `nonArrServices` logic replaced with explicit `piholeService` slot
- `DownloadActivity` component added inside Media tile: shows active arr downloads (radarr/sonarr/lidarr) with purple progress bars + SABnzbd speed/progress row; shows IDLE when nothing downloading

### NAS tile vertical bars (Task 2)
- `NasTileInstrument` replaced with new `NasGaugeColumn` component:
  - 8px wide × 60px tall track, fills from bottom
  - Glow shadow on fill (`0 0 4px color`)
  - Label above (9px mono), value below (10px with text-shadow)
- Top section: CPU + RAM + all volumes as vertical columns
- Middle section: disk temps as `flex-direction: column` cells with label (9px muted) + value (11px amber glow)
- Bottom section: Docker aggregate stats (DCK CPU / RAM) when `nasStatus.docker` exists

### NetworkInstrument polish (Task 3)
- BLOCKING status promoted to `fontSize: 20px, fontWeight: 600` with text-shadow glow; green when blocking, red when disabled
- "BLOCKING" sub-label rendered below in 8px muted
- QPM value promoted to `fontSize: 20px` with amber glow
- "QPM" sub-label rendered below in 8px muted
- ThroughputBar speed value span updated from 8px to 10px with `text-shadow: 0 0 6px currentColor` and inherits bar color
- Clients bar already uses `#4ADE80` with `boxShadow: 0 0 4px #4ADE80` — confirmed correct, no change needed
- Arrow indicators already use `text-glow` class — confirmed correct, no change needed

### Plex NowPlayingBanner stat colors (Task 3)
- Idle state: CPU=`#4ADE80`, RAM=`#00c8ff`, BW=`#C8C8C8`
- Collapsed strip: same colors
- Expanded drawer: same colors

### SABnzbd card cleanup (Task 3)
- `ServiceCard` now returns `null` immediately for `service.id === 'sabnzbd'`
- Banner header `DOWNLOADS` label and `SabnzbdLed` special case removed from the pihole banner condition
- `minHeight: sabnzbd ? '110px'` special case removed

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing] Removed unused `ServiceStatus` import from CardGrid.tsx**
- Found during: Task 2
- Issue: TypeScript import was included but unused after the rewrite
- Fix: Removed from the import statement
- Files modified: CardGrid.tsx
- Commit: 7bfab1f (fixed before commit)

**2. [Scope note] `dockerContainers` array does not exist in NasStatus**
- The plan mentioned checking for `nasStatus.dockerContainers` (per-container array). The shared type only has `docker?: NasDockerStats` (aggregate). Implemented aggregate display as the plan's stated fallback — `DCK CPU x.x% RAM x.x%` with glow styling.

## Verification Results

- `npm run build --workspace=packages/frontend` — zero TypeScript/build errors
- `npm run test --workspace=packages/frontend` — no test files, exits 0
- AppHeader no longer accepts `nas` or `nasConfigured` props
- SABnzbd does not appear as standalone card in CardGrid
- NAS tile renders full width in Row 1
- Media tile renders in Row 2 left column with download activity section
- Network/Pi-hole tile renders in Row 2 right column

## Known Stubs

None — all sections render live data when configured.

## Self-Check: PASSED

Files modified confirmed:
- `/Users/Oreo/Projects/Coruscant/packages/frontend/src/components/layout/AppHeader.tsx` — exists, slimmed
- `/Users/Oreo/Projects/Coruscant/packages/frontend/src/App.tsx` — exists, paddingTop 52px
- `/Users/Oreo/Projects/Coruscant/packages/frontend/src/components/cards/CardGrid.tsx` — exists, new layout
- `/Users/Oreo/Projects/Coruscant/packages/frontend/src/components/cards/ServiceCard.tsx` — exists, vertical bars + polish
- `/Users/Oreo/Projects/Coruscant/packages/frontend/src/components/layout/NowPlayingBanner.tsx` — exists, stat colors

Commits confirmed:
- `5b595ec` — Task 1: slim AppHeader
- `7bfab1f` — Task 2: NAS full-width + CardGrid restructure
- `300dc29` — Task 3: Variant C polish
