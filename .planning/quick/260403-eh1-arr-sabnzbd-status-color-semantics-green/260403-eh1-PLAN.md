---
phase: quick
plan: 260403-eh1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/styles/globals.css
  - packages/frontend/src/components/cards/ServiceCard.tsx
  - packages/frontend/src/pages/ServiceDetailPage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Arr/SABnzbd LED is green when service is online and idle"
    - "Arr/SABnzbd LED is red when service is offline"
    - "Arr/SABnzbd LED is yellow (amber) when service reports a warning/degraded state"
    - "Arr LED and download bar are purple when service is online AND actively downloading"
    - "SABnzbd progress bar is purple when queue is active (queueCount > 0 or progressPercent > 0)"
    - "Attention Required section in ArrDetailView retains amber for manual-import items and red for failed items"
    - "--cockpit-purple CSS token exists in :root"
  artifacts:
    - path: "packages/frontend/src/styles/globals.css"
      provides: "--cockpit-purple token in :root"
      contains: "--cockpit-purple"
    - path: "packages/frontend/src/components/cards/ServiceCard.tsx"
      provides: "Purple LED + bar when downloading in ArrInstrument; purple bar in SabnzbdInstrument"
    - path: "packages/frontend/src/pages/ServiceDetailPage.tsx"
      provides: "Attention section colors unchanged (amber manual-import, red failed)"
  key_links:
    - from: "globals.css"
      to: "ServiceCard.tsx ArrInstrument"
      via: "var(--cockpit-purple)"
    - from: "globals.css"
      to: "ServiceCard.tsx SabnzbdInstrument"
      via: "var(--cockpit-purple)"
---

<objective>
Add arr+SABnzbd-specific status color semantics: green=up, red=down, yellow/amber=warning or attention-needed, purple=downloading. Apply to the LED indicator and download bar in ArrInstrument and the progress bar in SabnzbdInstrument (ServiceCard.tsx). Add the --cockpit-purple CSS token to globals.css. Confirm the ArrDetailView attention section in ServiceDetailPage.tsx already has correct color semantics (no change expected there).

Purpose: Make download activity visually distinct from health state at a glance — a purple LED means "healthy AND working", not just "healthy".
Output: Three files updated; --cockpit-purple token added; ArrInstrument and SabnzbdInstrument use purple for download state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --cockpit-purple token to globals.css</name>
  <files>packages/frontend/src/styles/globals.css</files>
  <action>
In the `:root` block, add the purple token after `--cockpit-grey`:

```css
--cockpit-purple: #8B5CF6;
```

Use `#8B5CF6` — a muted violet-purple that reads clearly against the dark `--space-deep` background, distinct from the amber/green/red palette. Approx ~#9B59B6 was suggested; #8B5CF6 is slightly cooler and more readable at small LED sizes — use #8B5CF6.

Also add a matching `@keyframes arrDownloadPulsePurple` animation variant after the existing `arrDownloadPulse` keyframes, so the purple bar can have independent timing:

```css
/* Purple download pulse — used when arr/SABnzbd is actively downloading */
@keyframes arrDownloadPulsePurple {
  0%, 100% { opacity: 0.65; }
  50%       { opacity: 1.0; }
}
```
  </action>
  <verify>grep -n "cockpit-purple\|arrDownloadPulsePurple" packages/frontend/src/styles/globals.css</verify>
  <done>--cockpit-purple and arrDownloadPulsePurple exist in globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Apply purple download semantics in ServiceCard.tsx</name>
  <files>packages/frontend/src/components/cards/ServiceCard.tsx</files>
  <action>
Two instruments need changes:

**ArrInstrument** (lines ~79-179):

The LED color logic currently maps: online=green, warning=amber, offline/else=red. Extend this to add a fourth state: online+downloading=purple. The LED color derivation should become:

```tsx
const ledColor =
  service.status === 'online' && downloading
    ? 'var(--cockpit-purple)'
    : service.status === 'online'
    ? 'var(--cockpit-green)'
    : service.status === 'warning'
    ? 'var(--cockpit-amber)'
    : '#ff3b3b'
```

The status text row should remain unchanged (still shows "ONLINE" for online+downloading — the purple LED communicates the download, the text confirms the service is up).

In the download indicator row (the active download branch): the progress bar `background` should change from `'var(--cockpit-amber)'` to `'var(--cockpit-purple)'`, and its `animation` should change from `'arrDownloadPulse 1.4s ease-in-out infinite'` to `'arrDownloadPulsePurple 1.4s ease-in-out infinite'`. The label text `color` should change from `'var(--cockpit-amber)'` to `'var(--cockpit-purple)'`. The track background `rgba(232,160,32,0.15)` should become `rgba(139,92,246,0.15)` to match the purple.

**SabnzbdInstrument** (lines ~217-263):

SABnzbd shows a download progress bar. The bar should be purple when the queue is active (queueCount > 0 OR progressPercent > 0), amber when idle.

Derive an `isDownloading` boolean:
```tsx
const isDownloading = queueCount > 0 || progressPercent > 0
```

Then apply:
- Progress bar `background`: `isDownloading ? 'var(--cockpit-purple)' : 'var(--cockpit-amber)'`
- Progress bar track `background`: `isDownloading ? 'rgba(139,92,246,0.15)' : 'rgba(232,160,32,0.15)'`
- Speed label `color` (the `{speedMBs.toFixed(1)} MB/s` span): `isDownloading ? 'var(--cockpit-purple)' : 'var(--cockpit-amber)'`

The QUEUED count span stays `#C8C8C8` (neutral — it's a count label, not a state color).

Do NOT touch any other instrument (NasInstrument, PlexInstrument, PiholeInstrument). Do NOT touch the global StatusDot component.
  </action>
  <verify>grep -n "cockpit-purple\|isDownloading" packages/frontend/src/components/cards/ServiceCard.tsx</verify>
  <done>ArrInstrument LED uses purple when online+downloading; download bar uses purple; SabnzbdInstrument bar uses purple when active</done>
</task>

<task type="auto">
  <name>Task 3: Verify ServiceDetailPage.tsx attention color semantics</name>
  <files>packages/frontend/src/pages/ServiceDetailPage.tsx</files>
  <action>
Review the ArrDetailView attention section. Confirm:
- The section border and background use amber (`var(--cockpit-amber)` / `rgba(232,160,32,...)`) — this is correct for "user attention needed" (yellow).
- "Manual Import" type items use amber text — correct.
- "Failed" type items use red (`var(--cockpit-red)` / `rgba(255,59,59,...)`) — correct.

No color changes are expected here — amber is already the correct semantic for the attention section. However, make one cosmetic improvement: change the attention header span `color` from `'var(--cockpit-amber)'` to `'var(--cockpit-amber)'` with no change (confirm it is amber) — if the header symbol is the Unicode `⚠`, keep it; if it is a raw emoji, replace with the text `[!]` to avoid rendering inconsistency on the Pi touchscreen (emoji rendering varies on embedded browsers).

Specifically: change `⚠ Attention Required` to `[!] ATTENTION REQUIRED` for consistent mono-font rendering. Update the span `fontSize` from `'11px'` to `'11px'` (unchanged) and ensure `textTransform: 'uppercase'` is set (it already is).

That is the only change. No color changes to the attention section.
  </action>
  <verify>grep -n "ATTENTION REQUIRED\|\[!\]" packages/frontend/src/pages/ServiceDetailPage.tsx</verify>
  <done>[!] ATTENTION REQUIRED header renders in mono font; attention section colors are amber (border, bg, header) and red (failed items)</done>
</task>

</tasks>

<verification>
1. `grep -n "cockpit-purple" packages/frontend/src/styles/globals.css` — token present
2. `grep -n "cockpit-purple" packages/frontend/src/components/cards/ServiceCard.tsx` — used in ArrInstrument and SabnzbdInstrument
3. `grep -n "isDownloading" packages/frontend/src/components/cards/ServiceCard.tsx` — SabnzbdInstrument has the boolean
4. `grep -n "\[!\]" packages/frontend/src/pages/ServiceDetailPage.tsx` — attention header updated
5. `cd /Users/Oreo/Projects/Coruscant && npm run build --workspace=packages/frontend 2>&1 | tail -20` — no TypeScript errors
</verification>

<success_criteria>
- --cockpit-purple (#8B5CF6) token is in globals.css :root block
- arrDownloadPulsePurple keyframe exists in globals.css
- ArrInstrument LED is purple (not green) when service.status === 'online' AND downloading === true
- ArrInstrument download bar is purple with purple track background when actively downloading
- SabnzbdInstrument progress bar is purple when queueCount > 0 or progressPercent > 0; amber when idle
- ServiceDetailPage attention section is unchanged in color (amber border/bg, red failed items)
- Attention header changed from ⚠ emoji to [!] text for mono-font consistency
- No TypeScript compilation errors
- Global StatusDot component is untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260403-eh1-arr-sabnzbd-status-color-semantics-green/260403-eh1-SUMMARY.md` using the summary template.
</output>
```
